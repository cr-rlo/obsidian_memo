## HTML만으로는 부족했던 이유
- 구조만 있고, 조작은 없던 시절
- 정적 HTML 문서
- HTML문서는 마치 종이로 인쇄된 책처럼, 그저 읽는 것만 가능한 정적 콘텐츠
- 단순성, 빠른 로딩, 접근성이 좋다는 장점
- 상호작용석 부족, 참여도 감소, 업데이트가 어렵다는 단점
- 이 단점들을 수용하는 과정에서 자바스크립트 등장
- 자바스크립트는 직접 HTML 자체를 변경하여 조작할 수는 없으나 DOM을 조작하여 브라우저의 메모리 내 표현을 변경함
	- 브라우저가 HTML 문서를 읽어들인 후, 내부 메모리 상의 객체 기반 구조로 바꿔 저장한 데이터를 조작하게 됨
	- 이 객체 기반 구조가 바로 Document Object Model, 즉 돔
- DOM이란 다큐먼트 문서를 오브젝트 객체로 모델링한 구조
	- 텍스트로 작성된 HTML 문서를 브라우저가 읽고 각 요소들을 하나하나 객체화하여 트리구조로 메모리에 구성해 놓은 것
	- 정적인 마크업을 동적으로 조작 가능하게 만드는 중간다리 역할을 DOM이 함
- DOM 구조 피라미드
	- DOM은 HTML 문서를 트리 형태의 객체 구조로 바꾼 것으로 자바스크립트가 브라우저와 상호작용할 수 있는 창구
	- DOM은 우리가 작성한 HTML을 하나의 거대한 객체 트리로 변환해줌
	- DOM 트리의 각 노드는 HTML 요소를 나타내며 자바스크립트는 이 노드를 찾아서 웹페이지를 조작
- HTML 중첩 구조
	- HTML은 단순한 텍스트 문서처럼 보이지만 실제로는 중첩, 트리(nesting) 구조를 갖는 계층적 언어
	- 중첩은 부모 자식간의 관계로 작동
	- 들여쓰기와 같은 중첩 구조는 곧바로 트리 구조로 해석될 수 있음
	- 어떤 태그가 무엇의 안에 포함되어 있는지를 정확히 파악한 후, 각 태그를 부모, 자식, 노드로 구성된 객체 트리로 변환하는 것
- HTML은 문서 구조의 설계도 
- 자바스크립트는 이 구조를 조작하는 기능 담당자
- 이 둘 사이를 연결해주는 매개체가 DOM
## DOM은 무엇인가
- 돔은 브라우저가 HTML을 이해하기 위해 만들어낸 구조화된 결과이자, 자바스크립트가 브라우저와 소통할 수 있도록 해주는 공식적인 인터페이스(API)
- 브라우저는 HTML 파서라는 프로그램을 통해 이 텍스트를 한 줄씩 읽으며 의미 단위로 나눈 후, 그 기반 위에 구조화된 트리 구조를 만들어냄
- HMTL 파싱 피라미드
	- HTML 문자열이 네트워크를 통해 전달됨
	- 문자열을 문법적으로 분석함 (파싱)
	- 태그를 토큰 단위로 분리함
	- 토큰을 노드로 변환
	- 노드를 연결하여 트리 구조를 형성함
- 돔은 HTML을 그대로 보존하는 것이 아니라 HTML을 구조화된 객체의 트리로 변환하여 자바스크립트가 이를 제어할 수 있도록 만든 형태
- 우리는 이 구조를 콘솔에서도 확인할 수 있음
	- console.dir() : 자바스크립트 객체를 트리구조로 펼쳐서 속성과 메서드를 계층적으로 보여주는 도구
## HTML에서 DOM으로 
- 컴퓨터에 HTML코드를 적는 것은 웹페이지의 설계도를 만드는 것
- 사람이 보기에 이해하기 쉽지만 컴퓨터에게는 단순한 문자에 불과
- 브라우저는 이 HTML을 읽고 해석하여 하나의 객체 구조로 변환하는데 그 결과 만들어지는 구조물이 DOM
- HTML 문서 처리
	- 문서읽기: 브라우저는 HTML 문서를 맨 위에서부터 한 줄씩 차례로 읽음
	- 토큰생성: 각 태그를 만날 때마다 이를 토큰, 즉 작은 단위로 나눔
	- 노드생성: 열리는 태그를 보면 새 요소가 생겼다고 판단하여 새 노드를 만들고 닫히는 태그를 보면 요소가 끝났다고 판단하여 부모로 돌아감
		- 이 과정을 반복하면서 하나의 트리 구조 즉 부모와 자식 관계를 가진 구조를 만듦
	- 문서 렌더링: 브라우저가 처리된 문서를 표시함
## 구조와 API를 왜 분리해서 이해해야 할까
- 돔을 구성하는 핵심요소 구조와 인터페이스 역할을 하는 API
- 돔은 구조물 -> HTML로 작성된 코드는 브라우저에 의해 돔 트리라는 건물로 조립됨
- 자바스크립트는 공구 -> 자바스크립트의 메서드들은 돔이라는 건물을 수정하고 확장하는 도구
- 자바스크립트로 돔을 조작한다는 것은 구조 안의 내용을 바꾸는 것이고 이를 위한 도구가 돔 API
- 돔 API는 브라우저가 돔을 조작하기 위해 제공하는 도구
- 돔 API는 우리가 만든 함수가 아니라, 브라우저가 미리 정해놓은 공식 함수
	- 우리가 이름을 바꿀 수 없고 모든 브라우저에서 동일하게 작동해야 함
## 돔은 자바스크립트일까? 브라우저 안의 동작 원리
- 돔은 자바스크립트 언어 자체에 포함된 것이 아니라 웹 브라우저가 제공하는 기능
- 자바스크립트는 단지 이 돔이라는 기능을 사용할 수 있도록 허용받은 언어
- 자바스크립트는 객체를 다루는 일반적인 언어지만 브라우저는 HTML을 해석해 document, window 같은 특수 객체를 만들어 자바스크립트에 제공함
- 예를 들어 documnet.getElementById()와 같은 메서드는 자바스크립트 자체의 기능이 아니라 브라우저가 제공하는 돔 API 덕분에 사용 가능한 것
- 이러한 메서드는 자바스크립트 문법으로 접근하고 조작할 수 있지만 그 실체는 브라우저가 만들어 놓은 돔 구조와의 연결 다리, 즉 자바스크립트와 돔을 이어주는 브릿지 역할
- 자바스크립트는 원래 돔을 인식하거나 해석할 능력이 있는 언어가 아니며 단지 브라우저가 제공해주는 문서 객체를 다룰 수 있을 뿐임
- 돔은 브라우저가 HTML을 해석해 만든 문서 객체 트리
- 자바스크립트는 그 구조를 조작할 수 있도록 브라우저가 연결해준 API를 활용하는 도구이자 언어
## 돔을 둘러싼 두 가지 시선 - 객체인가 구조인가
- 돔을 이야기할 때 한 편으로는 트리구조로 보고 또 한 편으로는 객체처럼 다루는 방식을 사용 -> 돔은 구조이면서 동시에 객체들의 집합 즉 구조화된 객체 트리
- 객체로 보는 시선은 그것이 조작 가능한 속성과 메서드를 가진 실체임을 강조
	- 객체란 이름과 값을 가진 데이터 덩어리
- 구조로 보는 것은 그것이 웹 페이지의 계층적 조직을 나타냄을 강조
- 돔은 하나의 큰 트리구조를 이루고 있으며, 그 안의 각 요소는 객체로서 정보를 가지며, 기능을 수행할 수 있는 자바스크립트 객체로 표현됨
## document 객체는 어떻게 만들어지고 무슨 일을 할까
- document란 웹페이지의 구조와 구속 요소들을 한눈에 알 수 있도록 정리한, 브라우저가 자동으로 만들어주는 전체 지도
- 브라우저는 HTML을 트리 구조로 해석한 뒤, 이를 객체로 메모리에 저장함
- 이 메모리에 만들어진 트리 전체를 가리키는 것이 document
- 따라서 자바스크립틑로 무언가를 하려면 document부터 시작해야함
- document가 없으면 우리가 찾고 싶은 요소가 어디 있는지를 알 수 없기 때문
- document는 모든 요소 탐색의 출발점
- 브라우저에서 문서 전체를 대표하는 중앙 관리자 역할을 함

- HMTL을 브라우저에 띄우는 순간 브라우저는 그것을 해석해서 DOM 트리를 만들고 그 트리 전체를 대표하는 다큐먼트 객체를 자동으로 자바스크립트 코드에서 사용할 수 있게 전역에 등록해 줌
- document는 HTMLDocument 클래스의 인스턴스
- HTMLDocument라는 설계도(class)를 바탕으로 실제 메모리에 만들어진 객체(instance)라는 의미
- 브라우저가 HTML을 해석한 결과로 document라는 실제 DOM객체가 생성됨
## DOM은 왜 트리인가? - 부모, 자식, 형제 노드의 구조 이해
- 트리란 어떤 하나의 기준점 루트가 있고 그 아래로 여러 개의 항목이 가지처럼 나뉘는 형태
- HMTL 문서를 보면 중첩된 구조를 갖고 있고 브라우저가 이를 트리처럼 변환해서 이해함
- DOM이란 문서를 객체처럼 다루기 위해 트리 구조로 만든 것이며, 각 HTML 요소를 하나의 노드처럼 다룰 수 있게 해줌
- HTML문서를 자바스크립트 코드로 다룰 수 있도록 문서 전체를 객체 형태로 바꿔놓은 구조이기 때문에 DOM이라고 부름
- 이 트리를 구성하는 각각의 조각을 노드라고 함
	- div 태그 뿐 아니라 텍스트, 주석, 속성 등도 각기 다른 종류의 노드로 포함됨
- 부모노드: 어떤 노드가 다른 노드를 감싸고 있을 때, 그 감싸고 있는 쪽을 부모 노드라고 부름
- 자식노드: 반대로, 부모 노드 안에 포함된 노드들은 그 부모의 자식 노드라고 함
- 형제노드: 같은 부모를 공유하는 노드들은 서로 형제 노드 
- DOM 탐색을 위한 기본 속성들
	- .parentNode: 현재 노드의 부모 노드 찾기
	- .childNodes: 현재 노드의 모든 자식 노드 확인, 줄바꿈 텍스트도 # text로 반환
	- .firstChild/ .lastChild: 첫 번째, 마지막 자식 노드 가져옴
	- .nextSibling: 다음 형제 노드 찾기
	- .previousSibling: 이전 형제 노드 찾기
	- nextElementSibling: 줄바꿈 같은 텍스트 노드 때문에 예기치 않은 오류가 발생할 수 있으므로 이를 사용해서 요소만 반환하는 것이 더 안전함
- DOM 주요 노드 정리
	- Element 노드: 눈에 보이는 HTML태그, 건물의 뼈대
	- Text 노드: 태그 안의 글자, 건물 안의 내용물
	- Comment 노드: 보이지 않는 주석, 벽에 붙은 메모
	- Attribute 노드: 태그의 속성 정보, 건물 옆의 표지판
## getElementById()
- 가장 빠르고 기본적인 선택자
- id 속성값을 가진 단 하나의 요소를 가져오는 함수
- 특정한 하나의 요소만 정확하게 집어내고 싶을 때 사용하는 도구
- id는 문서 전체에서 단 한 번만 사용해야 함
	- getElementById는 여러 개의 id가 존재하더라도 가장 먼저 발견된 단 하나의 요소만 가져오기 때문
## getElementsByClassName()
- 같은 class 속성을 가진 모든 요소들을 한꺼번에 가져오는 메서드
- 여러 요소들을 반환하기 때문에 단일 dom요소가 아닌 HTMLCollection 객체로 결과가 반환됨
## get ElementsByTagName()
- 같은 태그 이름을 가진 요소들을 모두 찾아서 모아줌
- 예를 들면 < div >, < p > 등과 같은 태그 이름을 가진 요소들을 모아주는 것
- 여러 요소들을 반환하기 때문에 단일 dom요소가 아닌 HTMLCollection 객체로 결과가 반환됨
- HTMLCollection은 유사 배열로 인덱스로 접근하고 .length로 개수 확인은 가능하지만 forEach, map과 같은 배열 메서드는 사용할 수 없음
- 반복할 때는 주로 for 루프나 for..of 구문을 사용
- 전체 문서뿐 아니라 특정 요소 내부에서만 검색하도록 범위를 제한할 수도 있음
## querySelector - CSS 문법으로 단일 요소 잡아내기
- 웹 개발을 하다 보면 클래스명, 태그명, 아이디명 등을 조합해 특정 요소를 정확히 선택하고 싶을 때가 많음
- 그렇게 복잡한 요소를 선택하고 싶을 때 사용하는 것이 바로 querySelector()
- 가장 먼저 일치하는 단 하나의 요소를 반환
- CSS 선택자 문법을 그대로 사용할 수 있기 때문에, 클래스, 아이디, 태그, 조합 선택 등 직관적이고 유연한 선택이 가능함
- querySelector(#main) 혹은 querySelector(.btn) 처럼 CSS에서 사용하는 클래스 문법이나 아이디 선택자를 사용할 수 있음
- querySelector()의 가장 큰 강점은 여러 선택자를 자유롭게 조합할 수 있다는 것
- 더 정교하고 유연한 요소 선택이 가능함
- document.querySelector("#container .highlighted"); 와 같이 id가 container인 요소 내부에서 클래스 명이 highlighted인 첫 번째 요소를 한 줄로 선택할 수 있게 됨
- querySelector()는 특정 요소 내부에서도 사용이 가능함
	- 전체 문서가 아니라 범위를 제한해 요소를 선택할 수 있음
	- 복잡한 구조에서도 정확하고 효올적인 선택 가능
- querySelector()는 CSS 선택자 문법을 그대로 사용할 수 있어서 직관적이고 정확하게 원하는 요소를 찾을 수 있다는 장점
## querySelectorAll()
- 모든 일치 요소를 한꺼번에 선택할 수 있는 메서드
- 같은 구조나 스타일을 가진 요소들이 여러 개 반복되는 경우가 있는데 이 경우 querySelector()처럼 하나의 요소만 선택하는 방식은 매우 비효율적
- querySelectorAll()은 CSS 선택자에 일피하는 모든 요소를 한 번에 선택함
- querySelectorAll()의 반환값은 NodeList로 특정 클래스를 가진 요소들을 선택하면 NodeList형태의 목록이 변수에 담김
	- NodeList는 번호가 붙은 요소들의 집합처럼 보이지만, 진짜 배열은 아니기 때문에 forEach()는 가능하지만 map(), filter()과 같은 배열 전용 메서드는 사용할 수 없음
## HTMLCollection VS NodeList
- 여러 요소를 한꺼번에 선택할 때 자동으로 생성되는 구조이며, 겉보기엔 비슷하지만 실제 동작은 꽤 다르게 작동함
- 어떤 메서드를 사용하느냐에 따라서 반환 타입과 함께 DOM변화 반영 방식도 자동으로 결정됨
- HTMLCollection
	- getElemenstByTagName()
	- getElementsByClassName()
	- documents.forms()
	- document.images()
	- DOM 변화 반영 -> 실시간 갱신
	- 라이브 컬렉션
	- DOM이 바뀌면 목록도 함께 변함
	- 실시간 DOM 상태를 감지할 때는 매우 유용하지만 반복문 중에 DOM을 변경하면 요소의 순서가 밀리거나 건너뛰는 현상이 생길 수 있음 -> 순회 중 DOM 변경 시에는 주의가 필요
		- 이를 해결하기 위해서 HTMLCollection()을 배열로 복사해서 사용할 수 있음
		- 이때 사용하는 메서드가 바로 Array.from()
	
- NodeList
	- querySelectorAll()
	- childNodes()
	- getElementsByName()
	- DOM 변화 실시간 반영 하지않고 선택 시점의 상태 고정
	- 정적 컬렉션
	- 선택 당시 상태만 유지됨
	- DOM 변화와 무관하게 유지되기 때문에 반복 중 요소를 추가하거나 삭제해도 순서가 꼬이지 않음
	- 중간에 리스트가 바뀌는 혼란 없이 안정적으로 순회하며 조작할 수 있음
	- 특히 삭제 작업이나 DOM 변경이 동시에 일어나는 경우 한 번 정해진 기준으로 작업할 때 매우 유용
	- forEach나 .length 등 사용가능
## getElementsByTagName()
- HTML 문서 안의 특정 태그들을 한꺼번에 선택할 때 자주 사용됨
- 지정한 태그 이름을 기준으로 해당하는 모든 요소들을 한꺼번에 찾아줌
- 선택한 모든 요소가 HTMLCollection으로 반환
	- 배열처럼 보이지만 배열은 아니며, Live 상태이다
- HTMLCollection은 배열처럼 인덱스로 접근 가능하며 각 요소에 .textContent나 .style을 사용해 직접 조작할 수 있음
- " * " 를 사용하면 해당 요소 내부의 모든 태그요소를 한꺼번에 선택할 수 있음
## getElementsByClassName()
- 특정 클래스 이름을 가진 HTML요소들을 모두 한꺼번에 가져오는 메서드
- 단일 클래스뿐만 아니라 공백으로 구분된 여러 클래스 이름도 전달할 수 있음
- getElementsByTagName()와 비슷함. 어떤 요소를 가져오느냐의 차이
## querySelector과 querySelectorAll
- 이 두 메서드는 CSS 선택자 문법을 그대로 사용할 수 있기 때문에 복잡한 구조에서도 원하는 요소를 직관적으로 선택할 수 있음
- querySelector
	- 조건에 맞는 첫 번째 요소 하나만 선택
	- 중요한 점은 가장 먼저 매칭되는 요소 하나만 반환한다는 것
- querySelectorAll
	- 조건에 맞는 모든 요소들을 한꺼번에 선택
	- CSS 선택자에 맞는 모든 요소를 가져와서 NodeList로 반환
- 구조 선택자
	- 같은 태그가 반복될 때, 그 중에서 순서나 위치에 따라 특정 요소만 선택할 수 있게 해주는 CSS 문법
	- 첫 번째 요소만 선택하고 싶다면 .first-of-type 또는 :nth-of-child(1)과 같은 구조 선택자 사용 가능
- 속성 선택자
	- 조건을 추가해 더 정확하고 세밀하게 요소를 선택하고 싶을 때
	- 요소에 지정된 특정 속성과 값을 기준으로 보다 정밀하게 원하는 요소만 선택할 수 있도록 해줌
	- [ 속성이름 = "값" ] 형태로 조건을 걸 수 있어 동적 콘텐츠가 많은 페이지에서 정밀한 선택에 매우 유용함
## DOM 트리 구조를 따라 부모나 형제 요소를 구조적으로 찾기
-  .parentNode : 해당 요소가 포함된 상위 요소인 부모 요소를 의미
-  .previousElementSibling : 해당 요소 바로 앞에 있는 형제 요소 의미
	- DOM 트리에서 같은 부모를 공유하며 나란히 위치한 요소를 형제요소라고 함
-  .parentNode.children : 부모요소의 모든 자식요소들을 배열처럼 가져옴
	- 해당 노드의 자식 요소들이 HTMLCollection에 포함되어 반환됨
-  .children VS .childNodes
	- .children은 요소 노드만 필터링해서 반환함 
		- ex) < li >HOME< /li >, < li >ABOUT< /li > ...
	- .childNodes는 텍스트 노드, 주석 노드까지 포함한 모든 노드타입을 포함함
		- 반복문 사용 시 # text, 주석 노드 등이 섞일 수 있어 예상치 못한 오류 발생 가능성이 있음
		- < li >HOME< /li >, < li >ABOUT< /li >, # text(줄바꿈 공백)...
## 요소를 만들고 추가하는 방법 - appendChild(), prepend()
- appendChild(), prepend() 두 메서드는 새로 만든 DOM 요소를 부모 요소의 내부에 추가하는 역할
- 요소를 만들었다면 반드시 appendChild(), prepend() 둘 중 하나를 써서 DOM에 연결해야만 화면에 보임
- appendChild()는 맨 마지막에 추가
	- 줄 맨뒤에 조용히 합류하여 성능에 미치는 영향 최소화
- prepend()는 맨 앞에 새로운 요소 추가
	- 줄 맨 앞에 끼어들어 성능에 미치는 영향 증가
- 이런 DOM 추가 작업이 자주 반복되면 성능 저하가 발생할 수 있으므로 필요할 때만 최소한으로 조작하는 것이 중요함

- DocumentFragment
	- dom추가 작업의 반복으로 성능저하가 우려될 때 사용할 수 있는 방법
	- DocumentFragment는 눈에 보이지 않는 임시 작업 공간으로, 마치 미리 작업을 준비해두는 가상의 상자처럼 사용됨
	- 실제 dom에 영향을 주지 않고 여러 요소를 한꺼번에 만들어두었다가 최종적으로 한 번에 추가할 수 있어 성능이 매우 뛰어남
	- dom추가 하기 전 요소들을 DocumentFragment에 임시 저장
	- DocumentFragment내에서 요소들을 미리 생성
	- DocumentFragment를 통해 DOM에 한 번에 추가
- document.createDocumentFragment();
## 다양한 위치에 요소 삽입하기 - insertBefore, insertAdjacentHTML
- 리스트 중간에 새로운 항목을 끼워넣거나, 기존 요소의 바깥쪽에 구조를 삽입하거나 서버에서 받은 HTML 문자열을 빠르게 랜더링해야하는 경우
- 삽입 위치와 방식에 따라 메서드도 달라짐

- 안쪽에 넣을지, 바깥쪽에 넣을지
- 맨 앞, 중간, 맨 뒤 중 어디에 넣을지
- DOM 노드를 넣을지, 문자열을 넣을지

- insertBefore() - 기준 요소 앞에 삽입
	- parentNode.insertBefore( newNode, referenceNode );
	- parentNode: 요소가 삽입될 부모 노드
	- referenceNode: 기준이 되는 자식 노드
	- newNode: 새로 삽입할 요소
	- 결과적으로 newNode는 referenceNode 앞에 삽입됨
	- DOM을 변경하면 브라우저는 요소의 크기와 위치를 다시 계산하는데 이를 리플로우라고 부름
	- 이 과정이 자주 반복되면 리플로우로 성능 저하 가능성
- append() - 더 유연한 추가 방법
	- appendChild()와 유사하지만 append()가 더 유연함
	- appendChild() -> 하나의 DOM 노드만 추가 가능, 단일 삽입에 적합
	- append() -> 문자열 + 여러 개의 노드도 한꺼번에 추가 가능
	- append()는 리턴값을 반환하지 않기 때문에 메서드 체이닝에는 적합하지 않지만 그만큼 간결함
- insertAdjacentHTML - HTML 코드를 기준 요소의 인접한 위치에 삽입
	- element.insertAdjacentHTML( position, htmlString );
	- position에는 beforebegin, afterbegin, beforeend, afterend가 들어갈 수 있음
	- beforebegin
		- 대상 요소 바로 앞에 삽입
	- afterbegin
		- 첫 번째 자식으로 삽입
	- beforeend
		- 마지막 자식으로 삽입
	- afterend
		- 대상 요소 바로 뒤에 삽입
## remove, replaceChild, innerHTML - DOM 조작의 마지막 퍼즐과 실전 전략
-  .remove()
	- 간단하고 직관적인 방법으로 요소를 없앨 수 있음
	- 다만, remove는 비교적 최신 메서드라 구버전 브라우저 호환이 필요하면 parentNode.removeChild(node)를 대신 사용
- .replaceChild()
	- 특정 부모 요소 안의 기존 자식을 새로운 노드로 바꾸는 메서드
	- 기존 요소를 완전히 제거하고 새 요소로 바꿀 때 사용
	- 요소를 교체할 때는 리플로우가 반드시 발생
	- 자식 요소가 많거나 스타일이 복잡할수록 성능 부담이 커질 수 있음
	- parentNode.replaceChild(newNode, oldNode);
- innerHTML
	- 요소 내부의 HTML 구조 전체를 문자열로 읽거나 설정
	- 예를 들어 box 요소 안에 < strong > 태그가 HTML로 해석되어 삽입되고 단순한 문자열이 아닌 실제 DOM 구조로 변환되어 랜더링 됨
	- 브라우저는 이 문자열을 HTML 파서로 해석한 뒤, 기존 자식 노드를 전부 제거하고 새로운 DOM 구조를 생성함
	- 이 과정은 직관적이지만 무거운 연산이며 자식 요소가 많거나 복잡할수록 파싱 비용이 커지고 리플로우와 리렌더링이 발생할 수 있음
- textContent
	- 요소 내부의 순수한 텍스트만 다룸, HTML 태그는 무시
		- < strong > Hello! < /strong >이라는 문자 그대로의 텍스트가 삽입되며, 태그는 작동하지 않고 출력만 됨
		- 이 방식은 HTML을 해석하지 않고 텍스트 노드를 직접 교체하기 때문에 가볍고 빠르며 보안적으로도 안전함
## HTML 속성과 DOM 속성의 결정적 차이 - getAttribute vs .value의 진짜 의미
- html 요소는 다양한 속성(attribute)를 통해 추가 정보를 포함함
- 예를 들어 < img > -> src, alt ..., < input > -> value, placeholder ...
- 이런 속성들은 HTML 문서에서 태그 안에 직접 정의되며 요소의 동작과 표현을 제어
- getAttribute()
	- HTML 속성 값을 검색
- setAttribute()
	- HTML 속성 값을 설정하거나 수정
- 이들은 HTML의 정적인 초기 상태를 제어하는 데 사용됨
- attribute
	- HTML 문서에 명시된 초기값
	- DOM 생성 시 기본 상태를 정의
- property
	- 브라우저가 DOM 구성 중 동적으로 부여하는 상태값
	- 사용자 입력, js 조작에 따라 실시간 변화. 쉽게 말해 현재 상태의 값


- .value
	- 변경된 값을 보여줌
	- 입력 값 변화는 DOM 속성에만 적용되고 HTML 속성은 그대로 남아있는데 .value는 그 변경된 값을 보여주고 getAttribute("value")는 여전히 초기 HTML 값을 보여줌
## 클래스와 스타일을 다루는 가장 강력한 방법 - classList, style, 리플로우
- HTML: 구조
- CSS: 스타일
- javaScript: 화면을 동적으로 바꾸는 역할
- 예를 들어 버튼을 누르면 요소가 숨겨지거나 특정 영역이 강조되거나 테마가 전환되는 등의 동작은 클래스나 스타일 속성 조작으로 이루어짐

- DOM 요소의 클래스를 다루는 첫 번째 방법: className
	- 자바스크립트에서 클래스 조작을 시작할 때 가장 기본적인 방법은 element.className 속성을 사용하는 것
	- 클래스들은 공백으로 구분된 하나의 문자열로 다뤄짐
	- 이 속성은 요소의 모든 클래스 이름을 문자열로 반환하거나 설정함
	- 그러나 클래스 하나만 추가/ 제거하더라도 전체 문자열을 직접 수정해야함
	- 클래스가 여러 개일 경우 공백 오타 실수로 예기치않은 스타일 오류 발생 가능성
- classList
	- 클래스를 배열처럼 다룰 수 있게해줌
	- classList의 내부는 DOMTokenList라는 특별한 객체
	- 이 객체는 클래스 하나하나 토큰 처럼 잘라서 배열처러 저장하고 관리
- classList.add( "  " ) - 클래스를 새롭게 추가할 때
	- 해당 요소에 "" 클래스를 추가함
	- 이미 그 클래스가 있으면 중복없이 무시하고, 없으면 새롭게 추가
- classList.remove( "  " ) - 클래스를 제거할 때
	- 특정 클래스를 요소에서 없애고 싶을 때 사용
	- 클래스가 있으면 즉시 제거되고 없으면 아무 일도 발생하지 않음
- classList.toggle( "  " ) - 클래스를 켜고 끄는 스위치
	- toggle()은 클래스가 있으면 제거, 없으면 추가하는 메서드
	- 클릭할 때마다 클래스 전환 등
- classList.contains( "  " )  - 클래스가 있는지 확인
	- 특정 클래스가 현재 요소에 적용되어 있는지를 검사하는 메서드
	- 있으면 true, 없으면 false 반환
- style. - 직접 인라인 스타일을 적용할 수 있는 DOM 객체
	- style 속성은 HTML 요소에 인라인 스타일을 자바스크립트로 설정할 수 있게 해줌
	- 예를들어 box.style.background = "red";
	- 인라인 스타일은 우선순위가 높아 기존 CSS를 덮어쓸 수 있음
	- 그러나 유지보수가 어렵고 CSS 설계 패턴을 무시할 수 있기 때문에 정말 필요한 경우에만 사용
	- 또한 자바스크립트에서는 CSS 속성명을 카멜케이스 형태로 사용해야 함
	- DOM 요소에 style을 반복적으로 직접 사용하면 브라우저의 렌더링 트리에 영향을 줌
	- 
## Node VS Element - childNodes와 children의 진짜 차이
- DOM 노드의 유형
	- Element 노드: HTML 태그
	- Text 노드: 글자, 공백 등
	- Comment 노드: 주석
	- Attribute 노드
- Node
	- 모든 노드 유형을 포함
- Element
	- 요소 노드만 포함

- childNodes
	- 해당 요소의 모든 자식 노드를 <mark style="background: #FFB8EBA6;">NodeList</mark> 형태로 반환
	- 여기에는 요소 노드, 줄바꿈 공백 등의 텍스트 노드도 포함
- children
	- 오직 Element Node만 포함
	- 해당 요소의 자식 중 Element Node만 추출함
	- 공백이나 줄바꿈같은 textNode는 제외하고 실제 HTML 태그 요소만 담긴 <mark style="background: #FFB8EBA6;">HTMLCollection</mark>을 반환함
- 실무에서는 children을 더 자주 사용함
	- 자식 요소들을 반복문으로 처리하거나 조작할 때 childNodes는 줄바꿈 공백 같은 textNode 까지 포함되어 예기치 않은 동작이 발생할 수 있기 때문임

- firstChild
	- 모든 노드 유형을 반환
	- 가장 첫 번째 자식 노드가 텍스트일 경우 텍스트 노드가 반환될 가능성
- firstElementChild
	- 요소 노드만 반환

## DOM 노드 조작 핵심 정리
- 모든 것은 Node로 표현되며 우리가 자주 다루는 Element는 그 하위 개념
- childNodes: 모든 노드 포함, NodeList로 반환
- children: 오직 Element 노드만, HTMLCollection으로 반환
- firstChild: 첫 번째 노드 (공백, 주석 등 포함), Node로 반환
- firstElementChild: 첫 번째 HTML 요소만, Element로 반환
## 이벤트 객체의 실제 활용 - 좌표 추적, 키 입력, 복사 방지
- 실무에서의 event 객체 활용
	- 사용자가 입력한 값: event.target.value
	- 마우스 좌표: event.clientX, event.clientY
	- 누른 키보드 키: event.key, event,code, event.keyCode
	- 복사 붙여넣기 시도: copy, paste, cut 이벤트 감지 기능
- 주의해야할 점
	- 이벤트 핸들러(add 이벤트 리스너의 두 번째 인지인 함수)의 첫 번째 인자엔 항상 이벤트 객체가 자동으로 전달됨
	- 이름은 자유롭게 정할 수 있지만 가급적이면 event또는 e를 사용하는 것이 좋음
	- 이벤트 객체는 이벤트가 발생했을 때 브라우저가 '이 이벤트에 대한 정보 모음집'을 자동으로 만들어 넘겨주는 것
- 실용예제 = 텍스트 복사 감지 및 차단
	- document.addEventListener("copy", function(event){ alert('복사 금지입니다.'); event.preventDefault(); } );
	- preventDefault();
		- 해당 이벤트에 대한 브라우저의 기본 동작을 막는 메서드
		- 브라우저의 기본 동작을 차단하거나 이벤트 흐름을 제어할 수 있는 도구
## 마우스 이벤트 완전 정복
- mouseover
	- 마우스가 요소 위로 이동할 때 발생
	- 자식 요소 위로 이동할 때 반복 실행이 필요한 경우 적합
	- 자식 요소까지 포함해서 전체 영역에 반응하고 싶을 때 적합
- mouseeneter
	- 마우스가 요소 안으로 들어갈 때 발생
	- 요소에 처음 진입할 때 단 한 번의 실행이 필요할 경우 적합
	- 툴팁을 단 한번만 보여주고 싶을 때 적합
- mousemove
	- 마우스가 요소 위에서 움직일 때 발생
	- 마우스가 조금이라도 움직일 때마다 실행하는 상황에 적합
	- 커서를 따라 움직이는 배경, 그림자, 커스텀 효과
	- 너무 자주 실행되기 때문에 반복되는 로직이나 무거운 처리는 피하자
- mouseover은 자식 요소 진입 시에도 반복 실행되고 mouseenter는 처음 진입할 때 한번만 실행된다는 차이가 있음
## 이벤트 제거 - removeEventListener
- 등록된 아벤트 리스너는 직접 제거하지 않으면 계속 유지됨
- 요소에 이벤트를 등록하고 그 요소가 돔에서 사라져도 리스너는 여전히 메모리에 남아 성능저하, 메모리 누수, 예기치 않은 동작들을 발생시킬 수도 있음
- removeEventListener()
	- 이름 그대로 addEventListener()로 등록한 이벤트를 정확히 제거하는 역할
	- 이벤트를 정리하는 표준적인 방식이며, 메모리 누수와 성능 문제를 예방하는 핵심
	- 중요한 전제조건으로 제거하려면 동일한 콜백함수를 정확히 지정해야 한다는 점
	- 즉, 익명함수는 제거 불가( function(){}의 형식인 익명 함수 )
- 사용될 수 있는 사례
	- 사용자가 버튼을 한 번만 클릭하도록 제한하고 싶을 때
	- DOM 요소가 사라지기 직전
		- 동적으로 생성한 DOM요소가 삭제되거나 변경되기 전에 해당 요소에 등록된 이벤트를 반드시 정리해야 함
	- 같은 요소에 이벤트를 반복 등록할 때
		- 특정 UI 상태 변화에 따라 이벤트가 여러 번 호출되는 경우 등록 전에 반드시 명시적으로 기존 이벤트 제거
- 이벤트를 제거하는 방법에 once 옵션으로 제거하는 법도 있음
	- button.addEventListener( "click", handleClick, {once: true} );
	- 이 코드는 클릭시 handleClick을 실행한 뒤 브라우저가 자동으로 리스너를 제거하므로 일회성 인터랙션을 가장 깔끔하게 처리할 수 있는 방법임
## 실전 인터랙션 구현
- 툴팁
	- 사용자가 마우스를 요소 위에 올렸을 때 작은 박스 형태로 설명이나 추가 정보가 잠깐 표시되는 UI
- 사용자가 이미지 위에 마우스를 올리면 툴팁요소가 생성되고 화면에 표시, 마우스가 이미지에서 벗어나면 툴팁요소가 사라지는 구조를 구현해보자
- 이벤트 흐름과 함수 구성
	- 마우스 진입 시: mouseener -> showTooltip()
	- 마우스 이탈 시: mouseleave -> hideTooltip()
	- 두 개의 함수를 통해 툴팁의 생성과 제거를 명확히 분리

	- div.product: 상품 이미지를 감싸는 박스
	- img src: 내부에 있는 img, 실제 상품 이미지
		- position.relative: 툴팁의 위치가 기준 점이 됨
	- 자바스크립트 showTooltip
		- 새로운 div 요소 생성
		- 툴팁 텍스트와 스타일 설정
		- 이미지의 부모 요소에 툴팁 추가
		- 마우스 좌표 기준으로 위치 조정
	- 툴팁은 정적인 HTML에 미리 있는 것이 아니라 마우스 이벤트 발생 시 DOM에 새롭게 생성되어 동적으로 추가됨
	- 자바스크립트 hildeTooltip
		- mouseleave 이벤트 발생 시 호출
		- .tooltip 클래스를 가진 요소를 DOM에서 탐색 
		- 해당 툴팁 요소를 remove()로 제거
- 만약 사용자가 마우스를 빠르게 움직여 showTooltip이 반복 호출되어 툴팁이 여러 개 생성되거나 제거되지 않는다면?
	- 이를 방지하려면 자바스크립트 showTooltip 함수에 툴팁 중복 생성을 방지하는 조건문을 추가한다
	- 이미 .tooltip이 존재한다면 새로 만들지 않고 종료
## 마우스를 따라다니는 실시간 툴팁 - mousemove
- 기본 구조는 앞서 배운 툴팁과 같지만 이번에는 툴팁 위치를 고정하지 않음
- 마우스가 이미지 위에서 움직일 때마다 mousemove 이벤트로 툴팁 좌표를 실시간 갱신해, 커서를 따라다니는 효과를 구현함
- 툴팁 위치 갱신 로직을 moveTooltip()함수로 분리해 기존 툴팁 생성 함수는 그대로 두고 위치 제어만 따로 관리할 수 있게 만듦
	- function moveTooltip(){}
- 먼저 tooltip 변수를 전역에 선언함
	- tooltip은 showTooltip, moveTooltip, hideTooltip 세 함수가 모두 같은 DOM요소에 접근해야 하기 때문
	- tooltip을 전역에 선언하면 세 함수가 하나의 툴팁요소를 공유하며 조작 가능
- mouseenter
	- 마우스가 이미지에 처음 진입
	- showTooltip() 실행
	- 툴팁 생성 + moveTooltip으로 초기 위치 설정
- mousemove
	- 마우스가 이미지 안에서 이동
	- moveTooltip()이 매번 호출되어 좌표 갱싱
- mouseleave
	- 마우스가 이미지 밖으로 나감
	- hideTooltip() 실행
	- 툴팁 제거
- 성능 고려 포인트
	- mousemove는 초당 수십~수백번 호출
	- 마우스를 살짝만 움직여도 계속 이벤트 발생
	- mousemove 내부에 복잡한 계산 혹은 DOM요소를 매번 생성, 수정하는 무거운 작업을 포함하는 경우 성능 이슈 발생
	- 웹페이지가 버벅이거나 모바일, 저사양 환경에서 터치 반응이 저하될 수 있음
	- 이 경우 디바운싱(일정 시간 간격으로만 실행) 혹은 requestAnimationFrame()으로 브라우저 렌더링 주기에 맞춰 실행할 수 있음
## 이벤트 위임
- 하위 요소에서 발생한 이벤트를 부모(조상) 요소가 감지해서 처리하는 방식
- 직접 이벤트를 등록하지 않고 상위 요소에 위임하여 처리한다는 의미
- DOM 이벤트가 기본적으로 버블링 구조를 따르기 때문에 가능함
- 버블링이란?
	- 하위 요소에서 발생한 이벤트가 부모-> 조부모 -> 최상위 요소까지 차례로 전파되는 구조
	- 하위요소에서 클릭해도 상위 요소 하나에만 이벤트를 등록하면 그 이벤트를 감지하고 처리할 수 있음
- 장점
	- DOM 요소가 동적으로 추가될 때도 별도 처리가 필요 없음
	- 새로 생긴 요소도 이벤트를 자동으로 처리할 수 있음
	- 메모리를 덜 차지하고 등록, 해제도 간편함
	- 리스너가 하나면 메모리 효율도 좋고 관리도 쉬워짐
	- 실무에서 리스트 제어, 메뉴 클릭, 동적 추가된 항목 처리 등에 거의 필수적으로 사용됨
	- 반복적이고 변화가 많은 UI에 가장 효과적
- 주의할 점
	- event.target이 진짜 타겟인지 확인해야 함
	- 진짜 우리가 원하는 요소인지, 중간의 다른 요소를 클릭한 건 아닌지를 조건문으로 반드시 확인할 것
	- 이를 위해 tagName, classList.contains(), dataset 등 활용
	- 예를 들어 if(  event.target.tagName === "BUTTON" && event.target.textContent === "삭제" ) -> 버튼이 여러개 있어도 오로지 텍스트가 '삭제'인 버튼에만 반응
	- data-* 속성으로 HTML에서 선언한 값은 자바스크립티에서는 자동으로 dataset객체를 통해 접근할 수 있음 
		- event.target.dataset.action
- event.target.closest("li");
	- 클릭된 버튼의 위치에서부터 위로 올라가며 가장 가까운 li 요소를 찾는 메서드
	- parentNode/parentElement는 부모 요소 한 단계만 접근
	- 이벤트 위임처럼 깊이 중첩된 구조에서 특정 상위 요소를 정확히 찾고자 할 때는 closet()가 매우 유용하게 쓰임
- text.classList.toggle()
	- 이 메서드는 클래스가 없으면 추가하고 있으면 제거하는 방식으로 완료 상태를 시각적으로 표시하는 데 매우 유용한 방법
## 클릭만으로 상태가 바뀌는 리스트 만들기
- 할 일 목록 구현
	- 1. 항목 추가: 사용자가 목록에 새 항목을 추가함
	- 2. 데이터 저장: 새 항목이 데이터 배열에 객체로 저장됨
	- 3. 완료 토글: 완료 버튼을 누르면 해당 객체의 done 값을 true로 토글함
	- 4. 스타일 업데이트: UI가 항목의 완료 상태를 반영하도록 업데이트 됨
- 자바스크립트에서 구현할 것들
	- itemList: 리스트 항목이 표시되는 < ul >영역
	- itemInput: 사용자가 텍스트를 입력하는 입력창
	- addBtn: 항목을 추가하는 버튼
	- items: 실제 데이터를 저장하는 배열
	- idCounter: 각 항목에 고유한 ID를 부여하기 위한 숫자 카운터 -> 새로운 항목을 추가할 때 해당 ID를 DOM에도 반영하여 data-id로 변경
- addBtn.addEventListener("click", function(){ const text = itemInput.value.trim(); if (text === "") return })
	- 먼저 입력 값을 trim()으로 앞 뒤 공백 제거하고 값이 비어있다면 아무 동작 없이 함수 종료
- const item = {id: idCounter++, text: text, done: false,};
	- 새 항목 객체 생성
	- id, text, done 속성 포함
	- done은 완료 여부를 나타내는 논리 상태 값 (false로 초기화)
- items.push(item);
	- 이 객체를 items 라는 배열에 push()로 추가
- rederItem(item);
	- rederItem(item) 함수를 호출해 DOM에 렌더링 (실제 화면에 추가)
- itemInput.value = ""; 
	- 마지막으로 입력창 초기화
	- 입력창을 다시 비워줌
- function renderItem(item){ const li = document.createElement("li"); li.setAttribute("data-id", item.id); }
	- 리스트 함수를 화면에 렌더링하는 함수(DOM에 렌더링)
	- 주어진 item 객체를 기반으로 li 요소를 하나 생성함
	- 그 li 요소에는 data-id 속성이 설정되는데 이 값은 나중에 해당 항목을 식별하는데 쓰임
	- li.innerHtml = < span class = "item-text ${item.done ? "completed" : ""}"> ${ item.text }
		- 항목의 텍스트는 스팬 안에 들어가고, 완료 상태에 따라(item.done이 true라면) class "completed"를 토글함
	- < button data-action="completed">완료< /button>
	- < button data-action="delete">삭제< /button>
		- 그리고 버튼 2개 추가 
		- 각각 속성을 붙여서 나중에 이벤트 위임으로 클릭된 버튼의 역할을 판별함
- itemList.addEventListener("click", function(event){ const action = event.target.dataset.action; if (!action) return; } )
	- 핵심 로직인 이벤트 위임 리스너
	- 클릭 이벤트가 발생했을 때 data-id를 통해 해당 항목을 찾고 내부 데이터에서 done 상태를 바꾸거나 삭제할 수 있도록 함
	- itemList( < ul > )하나에만 클릭 리스너 등록
	- 내부 버튼이 눌려도 상위 요소가 감지
	- 클릭 이벤트가 발생하면 어떤 버튼이 눌렸는지 확인하고 data-action을 통해 삭제인지 완료인지 판단
	- 만약 버튼이 아닌 부분을 클릭했다면 return으로 종료
	- const listItem = event.target.closest("li");
		- 클릭된 요소에서 가장 가까운 조상 < li >를 찾음
		- 단순히 부모를 찾는 것이 아니라 선택자 조건 기반 탐색
	- const id = parseInt(listItem.dataset.it);
		- listItem.dataset.id는 해당 항목의 id를 문자로 읽어오므로 parseInt로 숫자로 바꿔줌
		- 이 id로 내부 배열에서 항목을 추적할 수 있게됨
	- const item = items.find( ( i ) => i.id === id ); 
		- items.find()는 내부 배열에서 id가 일치하는 객체 하나만 반환
	- if ( action === "delete" ){ items = items.filter( ( i ) => i.id !== id ); listItem.remove(); }
		- 이후, action이 delete라면 해당 객체를 배열에서 제거하고(filter()사용) , DOM에서는 remove를 사용해서 화면에서도 제거함
	- if ( action === "complete" ){ item.done = !item.done; const textEl = listItem.querySelector( ".item-text" ); textEl.classList.toggle("completed"); }
		- action이 complete라면 item 객체의 상태값 done을 반전시키고 .completed 클래스를 토글해 스타일을 시각적으로 변경함
		- 완료스타일은 css에서 .completed{ text-decoration: line-through; color: gray; }로 구현함
## 캡처링, 버블링, stopPropagation
- 어떻게 상위 요소가 하위 요소의 이벤트를 감지할 수 있을까
- 버튼만 눌렀을 뿐인데 왜 ul 같은 부모 요소에서도 그 이벤트를 감지할 수 있을까
- 그 답은 캡처링과 버블링에 있음

- 하나의 이벤트는 총 3단계를 거쳐 흐르게 됨
- 캡처링
	- 이벤트가 문서 최상단 루트 요소에서 시작하여 이벤트 타깃 요소를 향해 아랫방향으로 내려감
- 타깃
	- 이벤트가 실제로 발생한 요소인 타깃 요소에서 이벤트 실행
- 버블링
	- 타깃 요소에서 발생한 이벤트가 다시 위쪽으로 거슬러 올라가며 부모와 조상요소들이 이 이벤트를 감지
- 즉, 이벤트는 루트에서 시작해 하위로 내려가고 타겟 요소에서 실행된 뒤 다시 상위로 올라가며 전파되는 흐름을 갖는다

- 캡쳐링은 언제 발생하는가
	- DOM이벤트는 타깃으로 내려가기 전에도 실행될 수 있는데 이 단계가 바로 캡처링 단계
	- <mark style="background: #ABF7F7A6;">addEventListener()은 기본값으로 버블링 단계에서만 실행됨</mark>
	- addEventListener은 세번째 인자로 옵션 객체를 전달할 수 있는데 그 안에addEventListener(type, listener, {capture: ture})을 지저하면 캡처링 단계에서 실행되는 리스너가 됨
	- 이 설정은 평소에는 생략되기 때문에 많은 개발자들이 리스너는 항상 버블링에서만 실행된다고 착각하지만 사실 capture=true를 설정하면 이벤트가 타겟으로 내려오는 동안, 즉 전파 1단계 캡처링 중에도 코드를 실행시킬 수 있음
- stopPropagation
	- 이 메서드는 이벤트의 전파를 중단하는 데 사용됨
	- 예를 들어 버튼 클릭 이벤트에서 다음과 같은 코드를 추가하면 이제는 버튼을 클릭해도 btn의 리스너만 실행되고 이너 아우터 리스너는 실행되지 않음
	- 즉, 이벤트 전파가 타깃 단계에서 멈춘 것
- 더욱 강력한 stopImmediatePropagation()
	- 같은 요소에 여러 핸들러가 있을 때, 나머지 핸들러까지 전부 실행 중단
	- 자기 자신 안의 핸들로도 실행하지 못하게 막는 가장 강력한 제어 수단
- 정리
	- DOM이벤트는 흐름 속에서 동작한다
		- 단순히 누르면 실행되는 것이 아니라 캡처링 -> 타깃 -> 버블링 단계로 진행됨
	- 리스너는 시점과 위치가 중요하다
		- 언제, 어디에 이벤트를 걸어야 원하는 동작이 되는지 이해해야 함
	- 흐름을 멈추는 메서드들
		- stopPropagation -> 상위로의 전파 차단
		- stopImmediatepropagation -> 같은 요소의 나머지 리스너도 차단

## stopPropagation 실전 실험
- grandparent -> parent -> child 가 중첩된 구조
- 여기서 child 요소의 이벤트 리스너 내부에 event.stopPropagation을 추가
- 그럼 콘솔에는 오직 child 클릭만 출력되고 더 이상 상위 요소들이 출력되지 않음
- stopPropagation()이 타깃 단계에서 이벤트 흐름을 멈췄기 때문
- 이 메서드는 이벤트가 상위 요소로 올라가지 못하도록 전파를 차단함

- 실전에서 쓰이는 경우
	- 모든 자식의 클릭을 부모에서 감지하고 싶은데, 특정 자식만 예외로 하고 싶을 때
		- 리스트 전체에 이벤트 위임을 했지만, 어떤 버튼은 독립적으로 작동해야 할 경우
	- 모달 UI에서 바깥 영역을 클릭하면 닫기, 내부 클릭은 닫히지 않게 하고 싶을 때
		- 모달 배경은 클릭 감지, 모달 본문은 stopPropagation()
	- 중첩된 이벤트 구조에서, 하위 이벤트가 상위 로직을 방해하지 않도록 할 때
		- 하위 요소의 클릭이 부모의 form 제출이나 네비게이션 이벤트와 충돌할 때
- <mark style="background: #BBFABBA6;">stopPropagation()은 어느단계에서나 호출 가능하고 호출 이후의 전파는 모두 차단</mark>
- 예를 들어 버블링 중 호출하면 위로 올라가는 흐름이 차단되고 캡처링 중 호출하면 아래로 내려가는 흐름이 차단 됨(캡처링 단계는 이벤트가 문서 최상단에서 타깃 요소까지 내려가는 과정)

## taget VS currentTarget
- event.target
	- 이벤트가 시작된 요소 식별
	- 사용자가 직접 클릭한 요소-> 사용자이 입력이 실제로 발생한 요소
	- 즉 실제로 마우스가 닿은 곳
	- 따라서 closest(셀렉터)를 사용 -> 현재 요소에서 위로 올라가며 특정 셀렉터와 가장 가까운 조상요소를 찾아줌
	- DOM에서 이벤트가 처음 시작된 시점
- event.currentTarget
	- 이벤트 리스너가 연결된 요소 식별
	- 예를 들면 ul 즉 부모요소에 리스너가 달려있는 경우 ul을 칭함
	- 실제로 핸들러를 실행시킨 주체
- 이벤트는 버튼에서 시작해 버블링을 통해 ul까지 도달 -> target은 버튼, currentTarget은 ul

- 이벤트 위임을 사용할 때 실제로 클릭한 것은 버튼인데도 왜 우리는 상위 UL 요소에 이벤트를 등록했는가
- 그리고 상위 요소의 리스너에서 어떻게 진짜 클릭한 요소, 즉 버튼을 알아낼 수 있었는가
	-  event.target 과 event.currentTarget이라는 두 속성이 바로 핵심 해답
## 하나의 리스너로 모든 버튼 제어하기 - event.target과 dataset
- 여러 개의 버튼 중 어떤 버튼이 클릭되었는지 식별하고 그에 다라 서로 다른 동작을 수행하는 구조
- 부모 요소 하나에만 이벤트 리스너 등록
- event.target을 기반으로 클릭된 버튼의 정보를 판별 -> 실제 클릭된 요소 파악
- 조건 분기로 각 버튼에 맞는 동작 실행 -> action 값에 따라 각각 다른 기능 실행
- data-* 속성 -> 자바스크립트에서는 dataset객체로 자동 변환

- 버튼의 역할을 구분하는 data- 속성 활용
	- 각 버튼에는 data-action 이라는 커스텀 속성 설정(edit, delete, share등)
	- 이처럼 data-* 속성은 HTML5에서 도입된 사용자 정의 속성으로, javaScript에서 element.dataset으로 쉽게 접근 가능
- 이렇게 data-* 속성과 이벤트 위임을 조합하면, 직관적이고 유지보수에 강한 인터랙션 구조를 만들 수 있음
## 클릭 위치에 따라 다르게 동작하는 UI - target, closest, matches의 조합 실전 패턴
- 실전 UI에서 자주 발생하는 클릭 분기 상황
	- 커머스에서는 UI 상품 카드를 클릭해 상세 페이지로 이동하는 기능이 자주 필요함
	- 하지만 그 카드 안에는 삭제 버튼도 포함되어 있을 수 있음
-  <mark style="background: #ADCCFFA6;">- 한 요소 내부의 클릭 위치에 따라 다른 행동이 필요함</mark>
	- 카드 전체를 클릭했을 때는 상세페이지 이동
	- 삭제 버튼을 클릭했을 때는 삭제만 실행
	- 즉 같은 가트 안에서도 클릭한 위치에 따라 전혀 다른 동작이 실행되어야 함

- HTML 구조
	- 각 li에는 하나의 상품카드 
	- 내부에는 상품명을 나타내는 span
	- 삭제를 위한 button이 포함되어 있음
- 이벤트 위임 구조 만들기
	- 개별 카드마다 이벤트를 등록하지 않고 ul 상위 요소에 한 번만 이벤트 리스너 등록 -> 이벤트 위임
- 클릭된 실제 요소 찾기
	- event.target을 이용해 사용자가 클릭한 실제 DOM요소를 반환
	- 항상 카드단위로 동작을 처리해야 하므로 closest()를 사용해서 .card 클래스를 가진 가장 가까운 상위 요소 탐색
	- 카드 밖을 클릭했다면 card === null
- 클릭된 요소가 삭제 버튼인지 판단 matches()
	- event.target.matches( ".delete" )
	- .delete클래스라면 삭제 동작 실행
	- 그 외의 경우 -> 상세 페이지 이동 등 다른 동작 처리
## preventDefault VS stopPropagation
- 캡처링 - 위에서 아래로 이벤트 전달
- 버블링 - 타깃에서 위로 다시 이벤트 전파

- preventDefault()
	- 이벤트의 기본 동작을 중단함
- stopPropagation()
	-  현재 이벤트가 DOM 트리를 따라 위로 전파되는 것을 방지함
	- 예를 들어 자식 요소에서 클릭 이벤트 리스너를 막으면 부모 요소에 등록된 리스너는 실행되지 않음

- 위로 올라가는 전파만 막는 걸로 충분하지 않을 떄는?
	- 어떤 상황에서는 브라우저가 기본적으로 하려는 행동자체를 막고 싶을 수도 있음
	- 예를 들어 링크 클릭 시 이동, form 제출 등
	- 바로 그럴 때 쓰는 것이 preventDefault() -> 브라우저의 기본 동작(Default Behavior)을 차단
- 예시
	- 링크 클릭 시 홈페이지로 이동하는 화면 구현
	- 이 때, 자바스크립트 코드로 링크에 preventDefault()를 걸어주면 링크 클릭 시 이동이 막힘 (브라우저가 하려던 기본 동작이 막힘)

	- 반면에 stopPropagation()을 사용한 경우
	- 부모 요소로의 이벤트 전파는 차단되지만 링크 이동은 그대로 발생 -> 부모 요소에서 이벤트 리스너가 실행되지 않게 만들 수 있음
	- 즉, 브라우저 기본 동작은 막지 않음

  
















































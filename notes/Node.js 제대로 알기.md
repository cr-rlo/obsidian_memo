- 플랫폼의 구조와 기능들을 이해하기
## Node.js 란?
- 자바스크립트라는 언어를 알아듣는 프로그램
- 어떤 컴퓨터에 Node.js를 설치한다는 건, 해당 컴퓨터가 할 줄 아는 언어들의 목록에 자바스크립트를 추가한다는 의미
- 자바스크립트는 원래 브라우저라는 프로그램 안에서만 사용되던 언어
	- 자바스크립트는 HTML및 CSS 문서들과 함께 서버로부터 클라이언트로 전송된 다음, 클라이언트의 브라우저에서 실행되어 웹페이지의 요소들을 조작하거나 클릭 등에 의한 기능을 부여하는 데 사용되었음
## REPL
- Read - Eval - Print - Loop
	- 꼭 코드 파일을 따로 작성하지 않아도 터미널 등에 특정 언어의 코드를 입력하면 바로 결과가 나오는 환경 
	- 터미널에서 node를 열고 노드에서 바로 코드를 작성하여 결과보기 가능
## Promise와 async/await
- 비동기 코드를 보다 알아보기 쉽게 작성하는 데 사용됨
- 그 자체로 새로운 기능이 있는 것은 아니지만 콜백 함수로 짜던 코드를 보다 쉽게 작성하고 읽을 수 있도록 해줌
## 모듈
- 프로그래밍 코드를 한 곳에 작성하지 않고 기능과 목적별로 나누어 작성한 것
- 한 코드를 여러 곳에서 재사용할 수 있고 코드의 이해, 수정, 관리도 수월
## API
- 네가 이렇게 말하면 내가 이렇게 해줄게
- 두 소프트웨어가 서로 요청을 주고받을 때 각자 어떻게 응답과 요청을 주고받으면 되는지 메뉴판처럼 정해두는 것이 api
## RESTful API
- API의 다양한 형식들 중 오늘날 가장 널리 사용되는 것
- API를 통해 하는 일
	- create
		- POST 사용
		- 새 항목의 정보를 입력하려면, 용량이 클 수도 있는 정보를 담아보내야 하니 소포상자가 필요함
		- 해당 요청이 어느 리소스에 데이터를 추가하는 것인지만 명시하면 됨
	- read
		- 뭔가를 보여달라는 요청 -> 많은 정보가 필요 없음
		- GET 메서드 사용
		- 특정 조건으로 필터링하려면 query parameter 사용
		- 
	- update
		- PUT
			- 특정 항목의 정보를 전체적으로 대체할 때 사용됨
		- PATCH
	- delete
		- DELETE
		- 삭제할 항목의 식별자 넣어주면 됨
- URI는 어떤 자원에 관한 것인지 표현해야 하고 또 가능한 그것만 표현해야 함
- 그 외 어떤 일들을 하는지는 get, post, put, patch등의 메서드로 표현
- 요청을 보면 어떤 작업을 원하는지 바로 알 수 있음
- status codes
	- 성공적인 처리 success
		- 2xx 대 코드
	- client error
		- 4xx 대 코드
	- server error
		- 5xx 대 코드
		- 

## NODE.js의 stream 모듈 그리고 buffer
- stream
	- 대용량의 데이터를 다루기 위한 기술
	- 큰 데이터를 한 번에 옮기지 않고 작은 조각으로 나눠 물 흐르듯이 다루는 기술
	- import { Readable } from 'stream'
	- import { createReadStream } from 'fs'
	-  import { Writable } from 'stream'
	- import { Duplex } from 'stream'
		- 단방향만 가능한 redable과 writable과는 달리, Duplex 스트림은 읽기와 쓰기 모두 가능
	- import { createGzip } from 'zlib'
		- 데이터를 Gzip형태로 압축하여 내보내는 transform 스트림 생성
		- import { createCipheriv, randomBytes } from 'crypto'
			- crypto는 node.js에서 암호화 및 해시 기능을 제공하는 내장 모듈
		- 먼저 암호화의 알고리즘을 문자열로 설정한 다음
		- randomBytes 함수를 통해서 키와 초기화 벡터 생성
		- createCipheriv 함수에 algorithm, key, iv 이들을 인자로 전달해서 데이터를 암호화하는 transform 스트림을 생성
		- 압축 스트림과 암호화 스트림을 만든 후 이들을 읽기 스트림과 쓰기 스트림 사이에 파이핑( .pipe )
		- example 파일이 압축되고 암호화되어 새로운 파일로 저장됨
	- 



- buffer
	- 1. 옮겨지는 데이터 조각들이 일괄 처리를 위해 한곳에 쌓이는 것을 의미
	- 2. node.js에서 바이너리 데이터를 다루기 위한 객체
- Buffer
	- node.js에서 버퍼는 전역으로 제공되는 클래스
	- 따로 import할 필요없이 바로 사용하면 됨
	- Buffer from('')와 같이 문자열이나 배열 등을 매개변수로 받아서 buffer의 인스턴스를 생성 (버퍼형태로 바꾼 값)
	- 문자열의 각 문자가 16진수로 표현되어 버퍼로 담김
	- buffer.toString은 문자열 형태로 로그 출력
	- .alloc은 인자로 주어진 크기만큼의 버퍼 인스턴스 생성

## url, dns, util, os 모듈

- url
	- import { URL } from 'url'
	- URL 문자열을 파싱하고 조작하는 기능을 제공
	- URL 클래스의 인스턴스를 생성하여 기능들 사용
	- 생성자의 인자로는 다룰 URL의 문자열 값이 들어감
	- .protocol, .hostname, .origin, .port, .pathname .searchParams, .hash
		- 사용자가 직접 문자열을 파싱하지 않고도 URL의 인스턴스에 키로 접근하여 각각을 확인할 수 있음
	- .toString( ) 메서드를 사용하면 해당 인스턴스가 가진 값들에 따라 URL문자열을 반환
	- set, delete, append를 통해 매개변수의 값을 변경하고 지우고 추가할 수 있음
	- new URL( ) 인자로 첫 번째는 상대경로, 두 번째는 기본 URL을 넣어 생성하면 기본 URL에서 해당 경로로 들어간 결과로 만들어짐
	- import { fileURLToPath, pathToFileURL } from 'url'
		- 이 두 함수는 각각 파일 url을 경로로, 파일 경로를 파일 url로 바꿔줌
		- 파일 url은 웹 브라우저나 node.js같은 환경에서 로컬 파일 시스템의 자원에 접근할 때 사용하는 url 형식
		- 파일 경로는 운영 체제에서 파일이나 디렉토리의 위치를 나타내는 문자열
- dns
	- 도메인 이름 시스템에 관한 작업을 수행할 때 사용
	- 주로 네트워크 관련 작업이나 서버 관리, 디버깅 같은 상황에서 유용하게 쓰임
- util
	- 다양한 유틸리티 함수를 제공하는 내장 모듈
	- import { promisify } from 'node:util'
		- 인자로 전달된 콜백 기반 함수를 프로미스 기반으로 변환
	- import { inspect } from 'node:util'
		- 객체를 사람이 읽기 쉬운 문자열로 변환해주는 강력한 두고, 특히 디버깅이나 로깅에서 유용
	- import { deprecate } from 'node:util'
		- 어떤 함수가 문제가 있거나 새 함수로 대체되어 앞으로 사용되지 않도록 할 때 개발자에게 경고 메세지로 알리는 데 사용됨
- os
	- 운영체제와 관련된 정보 제공
	- import os from 'node:os';
## process
- Node.js에서 기본으로 제공되는 전역 객체로 현재 실행중인 node.js 프로세스에 대한 정보와 제어를 제공함
- 기본 객체이기 때문에 따로 임포트 할 필요가 없음
- pid, ppid, version, v8, arch, platform, execPath, cwd, cpuUsage, memoryUsage
- 환경변수
	- 환경변수란 컴퓨터의 전체 시스템 또는 특정 프로세스에, 실행중인 프로그램이 가져다 쓸 수 있는 값들이 들어있는 것
	- process.env
	- 현재 실행 중인 node.js 프로세스가 접근할 수 있는 환경 변수를 담고 있는 객체
## call stack
- 함수를 사용할 때마다 그와 관련된 정보들이 쌓이는 곳
- LIFO 형식
- 한계치 이상으로 함수가 연이어 호출되면 stack overflow
- call stack을 통해 오류가 발생한 지점을 알려주는 것을 stack trace
## 이벤트 루프
- 자바스크립트 코드가 실행되면서 동기/ 비동기 작업이 실행됨
- 코드가 실행되면 v8엔진에 의해 파싱되고, 동기 작업은 즉시 처리되며, 비동기 작업은 하위 계층으로 전달됨
- 동기적인 작업은 call stack에서 바로 실행됨
- 스택이 비워질 때까지, 대기가 모두 없어질 때까지 기다린 후 비동기 작업이 처리됨
- 파일관련 작업, http요청, 타이머 등의 비동기 작업들을 libuv로 넘김
- libuv는 node.js가 운영체제와 소통해서 빠르게 일을 처리하게 도와주는 도구
	- node.js가 컴퓨터에서 동작하려면 해당 컴퓨터가 사용하는 운영체제와 소통할 수있어야 함
	- libuv는 그 중간 다리의 역할을 맡아서 파일 열기나 네트워크 요청 등을 운영체제에 요청한 다음 그 결과를 다시 node.js에 전달하는 것
	- 따라서 libuv는 어떤 운영체제에 요청하는 지에 따라서 하는 일의 차이가 있음
	- libuv에 의해 비동기 작업들이 백그라운드에서 처리됨
- libuv에 속한 Thread Pool이란 여러 주방장이 주방장이 손님들의 주문을 동시에 처리하는 곳이라고 생각하면 됨 
	- 여기서 멀티 쓰레딩으로 비동기 작업들이 처리됨
- libuv에 속한 이벤트 루프는 손님들과 주방 사이를 오가는 직원과 같다고 생각하면 됨
	- 손님이 요리를 주문하면 대기표를 건내준 다음 주방에 주문을 전달하고, 수시로 주방을 확인하여 요리가 나오는대로 손님을 호출해주는 일을 맡음
	- 프로그램이 실행을 시작하며 이벤트 루프가 초기화 됨
	- 자바스크립트 코드가 처음 실행되면서 콜 스택에 쌓인 동기 코드가 처리되기 시작함
	- 동기 코드가 끝나면 비동기 작업의 결과나 대기 중인 콜백을 처리하기 위해 이벤트 루프로 넘어감
	- microtask 단계에서는 promise의 then 메소드와 process의 nextTick 함수와 같은 마이크로 태스크가 처리됨
		- 높은 우선순위를 가진 일을 처리하는 단계
	- timers Phase부터의 작업들을 이벤트루프는 쉬지 않고 처리
		- 여기서는 setTimeout이나 setInterval로 설정된 타이머가 만료되었을 때 해당 콜백을 실행
	- pending phase는 드물게 실행되는 단계로 여러 이유에 의해 이전 사이클에서 처리되지 못한 작업들이 마저 처리됨
	- poll phase는 가장 중요한 단계 중 하나로 파일 읽기, 네트워크 응답 등 비동기 작업의 완료를 기다리고 완료된 작업의 콜백을 실행함
	- check phase는 setImmediate 콜백을 실행하여 한 사이클을 마무리 한 뒤 다시 timer phase로 돌아감
	- 이벤트 루프는 마이크로 태스크와 각 페이즈를 체크하며 더 이상 처리할 작업이 없으면 종료됨
## 예제 코드 실행 과정
- 먼저 동기코드인 콘솔로그가 콜 스택에 담기게 됨
	- 다른 함수를 호출하는 코드가 아니므로 콜 스택에 들어가자마자 빠져나오면서 실행됨
	- 터미널에 가장 먼저 결과가 출력되고 콜 스택에서는 해당 코드가 사라짐
- 다음으로 process.nextTick() => 
	- 이 함수는 동기 코드들이 모두 실행된 직후 실행할 작업을 등록함
	- 콜 스택에 먼저 등록됨
	- 먼저 실행했던 콘솔로그와 마찬가지로 실행되면서 콜 스택에서 빠져나옴
	- 그리고 해당 메소드로 등록한 콜백이 메모리와 태스크 큐에 들어감
		- 메모리는 콜백 함수 자체가 저장되는 공간이고 태스크 큐는 이를 실행할 때를 스케쥴링 하기위한 대기열
	- 그 다음 promise의 then 메소드
		- 이 메소드 역시 콜스택에 올려졌다가 바로 실행되어 빠져나감
		- 해당 메소드에 인자로 전달한 콜백 함수가 역시 태스크 큐와 메모리로 들어감
			- then( ( ) => ) 여기서 ( ) => 이 부분이 태스크 큐로
	- nextTick과 then은 태스크 큐 내에서 다른 공간에 할당됨. 마이크로 태스크로 구분됨
	- 다음은 setTimeout 메소드
		- setTimeout의 콜백은 매크로태스크 큐에 속하는 타이머 큐로 들어감
		- 매크로태스크 큐는 마이크로태스크 큐보다 우선순위가 낮으므로 앞서 등록한 두 콜백보다 늦게 실행됨
	- 다음으로 setImmediate 메소드
		- checkPhase에서 실행할 콜백함수를 전달
		- 인자로 전달한 콜백함수는 체크 큐에 들어감
	- 다음은 파일로부터 비동기적으로 데이터를 읽어오는 readFile함수
		- 콜백함수는 매크로태스크 큐인 i/o 큐로 등록됨
	- 다음은 동기 코드인 콘솔로그
		- 동기코드이기 때문에 바로 터미널에 결과 출력
	- 이렇게 모든 동기코드가 처리된 후 태스크 큐에서 대기 중인 작업들이 우선순위에 따라 처리됨
		- 프로미스의 then 함수 실행되어 터미널에 겨로가
		- 그 다음은 nextTick 함수 처리
		- 매크로 태스크에 속하는 큐들이 차례로 처리
		- 마지막으로 setTimeout
## 여러 메소드
- setImmediate
	- 파일의 업로드는 poll phase에서 실행됨
	- 재귀 호출 사이에 파일 업로드가 이뤄지도록 하여 전체 업로드가 성공할 수 있도록 checkPhase에서 실행되는 setImmediate 함수 사용
- process.nextTick
	- 만약 db관련 코드를 처리하며 사용자 정보를 받아오는 상황이라고 하자
	- 정보를 받아오기 전에 해당 정보를 캐시에 업데이트 하는 일도 수행함
	- 해당 작업이 동기적으로 이뤄진다면 캐시 저장에 걸리는 시간만큼 사용자 정보의 반환도 늦어질 것
	- 이를 해결하기 위해 사용
	- 정보를 받아오는 작업을 nextTick으로 넘겨주면 이는 동기 코드들이 모두 실행된 다음 비동기적으로 진행됨
	- nextTick은 프로미스와 더불어 비동기 작업들 중 가장 최우선 순위를 갖는 microtask에 해당함
		- 따라서 어떤 작업이 중요한 다른 작업의 진행은 방해하지 않도록 비동기 작업으로 넘기되, 비동기 작업 중에서는 가장 먼저 처리되도록 하고싶을 때 사용






	













	
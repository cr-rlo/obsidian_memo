## P-NP
- 어떤 문제가 주어졌을 때 그 문제가 쉬운 문제인지 어려운 문제인지 
- 해결이 가능하다고 알려졌는지 아닌지 판단하는 척도
- 결정 문제
	- 어떤 문제가 주어졌을 때 yes/no 로 대답할 수 있는 문제
- 최적화 문제
	- 어떤 상황이 주어지고 거기서 최적의 해를 찾으라는 문제
	- 대부분의 최적화 문제는 결정 문제로 바꿀 수 있음

- P 문제
	- 결정 문제가 주어졌을 때 결정론적 튜링 머신을 사용해 다항 시간 내에 답을 구할 수 있는 문제
	- 결정론적 튜링 머신
		- 현재 상태에서 다음 상태로 이동할 때 다음 상태가 유일하게 결정되는 머신
		- 다음 상태가 유일한 상태로 결정되면서 문제를 해결하기 때문에 문제를 일방향으로 해결함
		- if문과 else문도 if문이나 else문이 수행될 때 다음 상태가 유일하게 결정되기 때문에 결정론적 튜링머신으로 처리 가능
	- 다항 시간
		- 문제가 해결되는 데 걸리는 시간을 다항식으로 표현할 수 있는 것
		- 단순하게 어떤 문제를 다항 시간 내에 즉, 상수시간이나 로그 시간, 다항 시간으로 풀 수 있는 문제가 P문제임
	- 지수 시간은 다항 시간보다 크기 때문에 다항 시간 내에 해결할 수 없음
- 정렬 문제
	- P문제
	- 정렬 문제를 해결하는 데 가장 쉽게 생각할 수 있는 알고리즘은 버블 정렬 알고리즘 -> O(n제곱)

- NP 문제
	- 결정 문제가 주어졌을 때 비결정론적 튜링 머신을 사용해 다항 시간 내에 답을 구할 수 있는 문제
	- 결정론적 튜링 머신으로 다항 시간 내에 답을 구할 수 없는 문제
	- 비결정론적 튜링 머신
		- 현재 상태에서 다음 상태로 이동할 때 다음 상태의 갯수가 유일하지 않은 머신
		- 현실에서 볼 수는 없고 있다고 가정한 것
		- 문제를 해결하는데 2의 n승과 같이 지수에 n이 있게 되면 이를 지수 시간이라고 함
		- 지수시간은 입력이 조금만 커져도 해결하는 시간이 매우 오래 걸리기 때문에 결정론적 튜링 머신으로 해결하기 힘듦
		- 비결정론적 튜링 머신은 어떤 선택지에서 유일한 상태가 아니라 여러 상태가 공존
	- NP 문제는 어떤 힌트가 주어졌을 때 결정론적 튜링 머신으로 '검증'은 가능함

- NP-hard 문제
	- 어떤 NP 문제들이 있을 때 이 모든 NP문제들을 다항 시간 내에 어떤 문제 A로 환원시킬 수 있다면 그 문제 A
	- 예를 들어 NP문제 A,B,C,D가 있는데 모든 NP 문제들이 어떤 문제 (A)가 해결 됐을 때 쉽게 해결될 수 있다면 NP문제들을 A로 환원시킬 수 있다고 말하고 이렇게 환원된 문제 A를 NP-hard 문제라고 함
- NP-complete 문제
	- NP-hard 문제에도 포함되면서 NP문제에도 포함되는 문제
	- NP-complete 문제는 어떻게든 문제를 해결할 수 있다는 것이 판별난 문제
	- 반면에 NP-hard 문제는 해결이 가능한지 가능하지 않은지도 판별이 나지 않은 문제

## 트리와 이진 트리
- 비선형 자료구조
- 트리구조
	- 하나의 노드에서 나무가 가지를 치듯이 내려가는 것
	- 계층구조를 표현하기 좋음
	- 노드
		- 데이터를 표현하는 덩어리
		- 데이터를 담는 가장 작은 단위
	- 간선(Edge)
		- 각 노드를 연결하는 선
		- 엣지로 연결된 노드에서 아래에 있는 노드는 위에 있는 노드의 자식
		- 자식의 관점에서 위에 있는 노드는 부모가 됨
	- 루트 노드
		- 트리에서 최상위에 있는 노드
	- 터미널 노드
		- 자식노드가 없는 노드
		- 터미널 노드를 제외한 노드는 인터널 노드

- 이진 트리
	- 이진 트리는 각각의 노드가 최대 두 개의 자식 노드를 가질 수 있는 트리
- 트리의 레벨과 높이
	- 트리는 자식 노드로 내려갈수록 레벨이 증가함
	- 루트 노드의 레벨은 1
	- 루트노드의 자식 노드는 루트 노드보다 레벨이 1 높음 -> 레벨 2
	- 트리에서 가장 높은 레벨을 높이라고 말함

- 포화 이진트리 VS 완전 이진트리
	- 포화 이진 트리
		- 트리의 최대 레벨에 있는 모든 터미널 노드가 꽉 찬 트리
		- 예를 들어 높이가 3인 트리에서 레벨 3위치의 터미널 노드가 꽉 차있다면 높이 3을 유지하며 새로운 노드를 삽입할 수 없으므로 포화 이진트리
	- 완전 이진 트리
		- 최대 레벨을 제외한 나머지 레벨에는 모두 채워져 있어야하고 최대 레벨의 노드들은 왼쪽부터 채워진 트리
		- 예를 들어 높이가 3일 때 레벨 2까지는 모든 노드가 채워져있고 레벨 3에 해당하는 노드들은 왼쪽부터 채워져있는 경우
## 트리와 이진 트리 - 구현
- 이진 트리의 추상 자료형
	- getData( )
		- 해당 트리(노드)의 데이터 리턴
		- 만약 루트 노드인 1에서 호출한다면 숫자 1 리턴
	- setData( )
		- 해당 트리(노드)의 데이터 설정
	- getLeftSubTree( )
		- 헤당 트리(노드)의 왼쪽 서브 트리 리턴
		- 만약 루트 노드인 1에서 호출한다면 2가 담긴 노드(트리)를 리턴
	- getRightSubTree( )
		- 해당 트리의 오른쪽 서브 트리를 리턴
		- 만약 루트 노드인 1에서 호출한다면 3이 담긴 노드(트리)를 리턴
	- setLeftSubTree( )
		- 해당 트리의 왼쪽 서브트리를 해당 트리로 설정
	- setRightSubTree( )
		- 해당 트리의 오른쪽 서브 트리를 해당 트리로 설정
	- preOrderTraversal( )
		- 해당 트리를 전위 순회하는 기능
	- inOrderTraversal( )
		- 해당 트리를 중위 순회하는 기능
	- postOrderTraversal( )
		- 해당 트리를 후위 순회하는 기능

- 순회
	- 전위 순회
		- 루트 노드 -> 루트의 왼쪽 서브 트리 -> 루트의 오른쪽 서브 트리
	- 중위 순회
		- 루트의 왼쪽 서브 트리 -> 루트 노드 -> 루트의 오른쪽 서브 트리
		- 루트를 중간에 출력했다고 해서 중위 순회
	- 후위 순회
		- 루트의 왼쪽 서브 트리 -> 루트의 오른쪽 서브 트리 -> 루트 노드
		- 루트 노드를 마지막에 출력했다고 해서 후위 순회
- 트리의 전체 구조를 출력하는데 재귀와 순회가 쓰임
## 이진 탐색 알고리즘
- 이진 탐색
	- 성능은 O(logn)
	- 매번 범위를 반으로 줄여가면서 탐색하는 것
	- 단점은 데이터가 정렬되어 있어야 한다는 점
## 이진 탐색 트리
- 해시 테이블 처럼 잦은 데이터 삽입, 삭제에 강력
	- 해시테이블
		- 키를 넣으면 그 값이 들어있는 위치를 즉시 알려주는 방식
		- 
- 검색에도 빠른 성능
- 데이터들을 순서대로 정렬해 저장하고 데이터를 저장할 공간을 제외한 다른 메모리 공간이 필요하지 않기 때문에 메모뢰 사용량도 더 적음
- 기본적으로 이진 트리의 모습을 하고 있음
- 이진트리에서 몇 가지 조건이 추가되면 이진 탐색 트리
	- 중복된 노드가 없어야 함
	- 특정 노드의 왼쪽 자식 노드는 그 노드의 값보다 항상 작은 값들로만 이뤄짐
	- 특정 노드의 오른쪽 자식 노드는 그 노드의 값보다 항상 큰 값들로만 이뤄짐
- 이진 탐색 트리의 기능
	- 데이터 삽입
	- 데이터 제거
	- 데이터 탐색
## 이진 탐색 트리 - 삽입과 구현
- insert(data) - 삽입
- search(targetData) - 탐색
- 그 외 제거에 필요한 여러 함수들

## 이진 탐색 트리 - 제거 구현
- 이진 탐색 트리는 단순히 노드만 제거한다고 되는 것이 아님
- 여러 상황으로 나눠서 처리해야 함
	- 터미널 노드 제거
		- 터미널 노드는 자식 노드를 가지고 있지 않기 때문에 단순히 부모 노드와 연결만 끊어주면 간단하게 제거됨
	- 자식 노드가 하나만 있는 노드를 제거하는 경우
		- 제거할 노드와 부모 노드의 연결을 끊고 부모 노드와 제거할 노드의 자식 노드를 연결해줘야 함
	- 자식 노드가 두 개인 노드 제거
		- 자식 노드가 두 개인 노드를 제거하면 왼쪽 자식 트리와 오른족 자식 트리 모두를 잃어버리게 됨
		- 자식 노드 중 어느 하나가 제거한 노드가 있었던 위치에 대신해서 와야함
		- 이 때 이진 탐색 트리의 조건을 무너뜨리지 않는 노드가 와야함
		- 먼저 왼쪽 노드에서 가장 큰 값을 제거한 노드의 위치로 이동시키는 경우
		- 혹은 오른쪽 노드에서 가장 작은 값을 제거한 노드의 위치로 이동시키는 경우
		- 둘 중 어느 것을 선택해도 괜찮음

	- 제거하려는 노드의 왼쪽 노드에서 가장 큰 값을 선택하는 경우
		- 먼저 제거하려는 노드의 왼쪽 자식 노드로 이동
		- 그 후 오른쪽 노드로 계속 가서 가장 큰 값의 노드를 찾음
		- 그렇게 찾은 가장 큰 값의 노드를 처음에 제거하려했던 노드에 덮어써줌
		- 그 후 제거하려던 노드의 왼쪽 자식 노드의 오른쪽 자식 노드를 가장 큰 값의 왼쪽 자식 노드였던 값으로 설정해줌
- 이진 트리는 컴파일러가 사용하는 구문 트리를 만들거나 jpeg, mpeg 파일을 만드는 압축 알고리즘인 허프만 트리를 만들 때 유용하게 쓰임
- 이진 탐색 트리가 잘 구현되지 않으면 O(n)의 성능
	- 예를 들면 이진 탐색트리가 한 방향으로만 연결된 경우 -> 이는 연결리스트와 같은 모습
	- 데이터를 잘 삽입하면 O(logn)의 성능
		- 완전 이진트리나 포화 이진 트리처럼 트리가 균형을 이루고 있어야함
		- 삽입이나 제거할 때 균형을 유지하는 방법들이 있음
		- 이를 자가균형이진탐색트리라고 함
## AVL 트리
- 트리의 균형을 유지하기 위해서는 균형이 맞는지 맞지 않는지 구분할 수 있아야 함
- 왼쪽 자식트리의 높이와 오른쪽 자식 트리의 높이가 최대 1만큼 차이 나도록 허용하고 이보다 더 차이가 난다면 균형이 무너졌다고 판단함
	- 루트 노드를 기준으로 왼쪽 서브트리와 오른쪽 서브트리의 높이 비교
	- 오른쪽 서브트리의 왼쪽 서브트리와 오른쪽 서브트리의 높이 비교
	- 왼쪽도 마찬가지
	- 이렇게 비교하는 과정을 거친 후 높이가 2이상 차이나는 서브트리가 있다면 서브트리의 균형이 맞지 않으므로 전체 트리도 균형이 깨졌다고 봄
- 균형이 맞지 않는 트리의 균형만 맞춰주면 전체 트리의 균형이 맞아짐
	- 트리의 구조에 따라서 오른쪽 또는 왼쪽으로 회전시키고 혹은 두 번 이상 회전시키는 등의 행동으로 균형을 맞출 수 있음
	- 
- 회전의 종류
	- LL 회전
		- 왼쪽 서브트리와 오른쪽 서브트리의 높이 차가 1을 초과하는 경우
		- 즉, 왼쪽 서브트리보다 오른쪽 서브트리의 높이가 더 높을 경우
		- 루트 노드를 왼쪽으로 회전시키면 균형을 맞출 수 있음
	- RR 회전
		- LL회전이 필요한 상황의 반대
		- 오른쪽 서브트리보다 왼쪽 서브트리의 높이가 더 높은 경우
		- 루트 노드를 오른쪽으로 회전시키면 균형을 맞출 수 있음
	- LR 회전
		- 왼쪽으로 먼저 꺾였다가 중간에서 안쪽으로 꺾인 구조
		- 예를들어 왼쪽 서브트리의 높이는 2이고 왼쪽 서브트리의 높이는 0
		- 이런 상황에서 왼쪽 서브트리의 루트를 왼쪽으로 회전
		- 여기서 루트노드를 오른쪽으로 회전
	- RL 회전
		- 오른쪽으로 먼저 꺾였다가 안쪽으로 꺾인 구조
		- 왼쪽 서브트리는 없으므로 높이가 0이고 오른쪽 서브트리의 높이는 2
		- 이런 상황에서 오른쪽 서브트리의 루트 노드를 오른쪽으로 회전
		- 그 후 루트 노드를 왼쪽으로 회전
## AVL 트리 구현하기
- 추상 자료형
	- insert(targetRootNode, data)
	- remove(targetRootNode, data, parentNode = null)
	- 이진 탐색트리의 구조를 그대로 사용하되 insert, remove를 새로 구현
	- 다른 점은 data이외에 node를 가리키는 인자가 추가되었다는 점
	- AVL트리에서 insert와 remove는 재귀함수이기 때문
- AVL 트리에서 이 함수를 구현하기 위한 보조함수들이 필요
	- getHeight(node)
		- 해당 노드의 높이를 구하는 함수
	- updateHeight(node)
		- 해당 노드의 높이를 새로 계산하는 함수
	- getBalanceFactor(node)
		- 해당 노드가 균형을 잃었는지 체크하기 위한 함수
	- rotateLeft(node)
		- 해당 노드를 LL회전 시키는 함수
	- rotateRight(node)
		- 해당 노드를 RR회전 시키는 함수
	- rotation(targetNode, data)
		- 실제로 LL, RR, LR, RL 회전을 시키는 함수
		- 이 함수에서 위에 rotateLeft, rotateRight 함수가 쓰임
	- getUnBalanceNode(targetRootNode)
		- 균형이 무너진 트리에서 균형을 무너뜨린 원인이 된 노드를 찾는 함수
	- removeHelper(deletingNode, data, parentNode)
		- 이진 탐색 트리에서 remove 함수 내에 특정 부분과 거의 똑같은 역할을 함
- insert 함수
	- 하향식 방법
		- insert함수가 호출되면 루트 노드부터 시작
		- 삽입하려는 데이터와 삽입하려는 위치의 루트 노드와 값을 비교하여 작으면 왼쪽, 크면 오른쪽으로 갈 것을 판단
		- this.insert(targetRootNode.getLeftSubTree(), data)를 통해서 자식 노드를 향해 계속해서 함수 다시 호출
		- 맨 위(루트)에서 결정을 내리고 아래로 작업을 위임
	- 상향식 방법
		- this.updateHeight(targetRootNode)
		- 삽입할 때는 위에 말했듯이 null을 만날 때가지 끝가지 내려가서 새 노드를 만드는 하향식 방법을 사용
		- 재귀함수가 종료되면서 부모 노드로 돌아감
		- 부모 노드로 돌아와 높이 업데이트하는 함수 실행
		- 가장 밑에 새로 생긴 노드부터 거꾸로 루트 노드까지 올라오며 상향식으로 높이 계산
- 정리
	- insert함수가 호출되면(데이터를 삽입할 때), 함수는 루트 노드부터 시작하여 null(빈공간)을 찾을 때까지 자기 자신을 계속 호출함
	- if문을 통해서 빈자리를 찾아감
	- 재귀 호출을 반복하다 기저 조건인 targetRootNode === null인 지점에 도달하면, 즉 빈자리를 찾았으면 새로운 노드 객체가 메모리에 생성되고 이 노드가 자신을 호출했던 바로 위 단계(부모 노드)의 함수로 전달됨
	- 함수가 종료되며 위로 복귀(상향식 연결 및 업데이트)

	- null을 만나면 실제 노드 만들고 자기를 호출한 부모에게 자신을 보냄
	- 자식이 보낸 노드를 set을 통해서 왼쪽이든 오른쪽이든 연결
	- 높이 업데이트, 로테이션 하고 반환
	- 그다음 부모에게 전달 이 과정 루트까지 반복


- remove와 removeHelper 함수
	- remove는 재귀적으로 동작
	- 데이터가 현재 노드보다 작으면 왼쪽으로, 크면 오른쪽으로 계속 내려감
	- 데이터가 일치하는 노드를 찾으면 removeHelper에게 제거 명령
	- 그 결과물로 삭제 후 대체된 노드를 받아옴
		- 대체된 노드는 삭제한 노드의 자식 노드
	- 삭제 직후 함수가 종료되면서 다시 부모쪽으로 거슬러 올라감
		- 높이 업데이트
		- 균형 체크
		- 회전
		- 등을 수행하면서 부모로 거슬러 올라감
	- removeHelper 함수
		- 삭제할 노드를 찾은 경우 자식의 상태에 따라 3가지 제거법 수행
		- 1. 자식이 없는 경우(터미널 노드)
			- 단순 삭제 
			- 연결을 끊고 null 반환
		- 2. 자식이 하나인 경우
			- 삭제할 노드가 빠지고 대체할 노드로 삭제할 노드의 자식이 옴
		- 3.자식이 둘 인 경우
			- 자식이 둘인 노드를 지우면 둘 중 어느 값을 대체할 노드로 삼아야할지 정해야 함
			- 보통 왼쪽 서브트리에서 가장 큰 값을 가진 노드를 대체할 노드로 설정
			- 대체할 노드를 삭제할 노드 자리로 복사해옴
## Red - Black 트리
- AVL트리는 Red-Balck트리보다 더 엄격하게 균형을 유지
	- 따라서 트리에 데이터 삽입과 제거 시 더 많은 회전이 발생
	- 따라서 삽입과 제거의 속도가 느림 그러나 검색의 속도는 더 빠름
- red-black트리는 좀 더 느슨하게 균형을 유지
	- 삽입과 제거 시 더 적은 회전을 하기 때문에 성능이 좋음
	- 그러나 검색 시 더 많은 시간이 걸릴 수도 있음
- 삽입과 제거 / 검색 중 어느 기능을 더 빈번하게 사용할지에 따라 어떤 트리를 사용할지 선택

- Red-Black트리의 규칙
	- 모든 노드는 빨간색이나 검은색 두 가지 색 중 하나를 가지고 있음
	- 루트노드는 항상 검은색
	- 모든 터미널 노드는 검은색(NIL 노드)
	- 노드가 자식노드를 가지고 있지 않으면 자식 노드 자리에 NIL노드라는 것을 만들어 넣음
		- 따라서 모든 터미널 노드는 NIL노드가 되고 이는 무조건 검은색
	- 연속해서 빨간색 노드가 올 수 없음
	- 검은색 노드는 연속해서 올 수 있음
	- 루트노드에서 터미널 노드인 NIL 노드까지 모든 경로에는 같은 수의 검은색 노드가 있어야 함
- Red-Black트리의 높이
	- 원래 우리가 알던 트리의 높이 개념
	- 여기에 하나의 높이 개념이 더 있음
	- Black Height
		- 루트노드부터 터미널(NIL)까지 경로에 존재하는 검은색 노드의 수를 말함

- 삽입
	- 이진 탐색 트리처럼 데이터를 삽입하고 균형을 잡아줌
	- 균형을 잡아줄 때 AVL트리처럼 회전도 하지만, 노드의 색깔을 다시 칠하는 Recoloring도 진행
	- red-black트리는 새로운 노드를 삽입할 때 무조건 빨간색으로 칠함
	- rebalance AfterInsertion 함수를 호출해 균형을 잡아줌
- <mark style="background: #FFF3A3A6;">rebalance AfterInsertion 함수가 처리해야하는 것</mark>
	- 새로운 노드가 루트노드인 경우, 즉 트리에 데이터를 처음 삽입하는 경우
		- 새로 삽입한 노드는 무조건 빨간색이니 루트노드가 항상 검은색이라는 규칙을 위반하지 않도록 색을 검은색으로 바꿔줌
	- 부모노드와 삼촌노드가 빨간색인 경우
		- 삼촌노드는 부모노드의 형제노드
		- 이 경우, 부모와 삼촌노드를 검은색으로 할아버지 노드를 빨간색으로 칠함
		- 그러나 만약, 증조할아버지 노드가 빨간색이라면 연속해서 빨간색 노드가 올 수 없다는 규칙을 어기게 되므로 할아버지를 대상으로 규칙을 위반하지 않을 때가지 재귀적으로 올라가면서 색을 바꿔줌
	- 부모노드는 빨간색, 삼촌노드는 검은색, 새로운 노드는 안쪽 손자인 경우
		- 안쪽 손자란 할아버지 노드를 기준으로 부모노드, 새로 삽입된 노드의 모양이 삼각형을 이룬 경우
		- 부모노드가 빨간색이기 때문에 연속해서 빨간색 노드가 올 수 없다는 규칙을 위반
		- 부모노드를 삽입된 노드의 반대쪽으로 예를 들어 새로 삽입된 노드가 부모 노드의 왼쪽에 삽입됐다면, 부모노드를 오른쪽으로 회전
		- 그리고 부모노드가 회전했던 반대방향으로 할아버지 노드 회전
		- 그리고 새로 삽입된 노드를 검은색, 할아버지 노드를 빨간색으로 바꿔줌
	- 부모노드는 빨간색, 삼촌노드는 검은색, 새로운 노드는 바깥쪽 손자인 경우
		- 바깥쪽 손자는 삽입된 노드가 할아버지 노드, 부모노드와 일자로 연결된 모양
		- 먼저 할아버지 노드를 삽입된 노드의 반대방향으로 회전
		- 그리고 부모노드는 검은색으로, 할아버지 노드는 빨간색으로 바꿔줌
- 제거
	- 검은색 노드가 제거될 때 black height가 달라지는 것이 문제가 됨
	- 루트노드에서 터미널 노드인 NIL노드까지 모든 경로에는 같은 수의 검은색 노드가 있어야하기 때문
	- 제거는 3가지 상황으로 나뉨
	- 1. 제거할 노드의 두 개의 자식노드가 모두 NIL노드인 경우
		- 단순히 제거할 노드를 제거해주고 제거할 노드의 부모 노드의 오른쪽 자식노드를 NIL노드로 만들어주면 됨
	- 2. 제거할 노드의 자식 노드 중 한 개가 NIL노드인 경우
		- 해당 노드(제거할 노드)를 제거해주고 제거한 노드의 자식 노드가 제거한 노드를 대체하면 됨
	- 3. 제거할 노드의 자식에서 NIL노드가 한 개도 없는 경우
		- 제거할 노드를 제거해주고 왼쪽 자식 노드에서 가장 큰 값의 노드가 제거할 노드를 대체하면 됨
		- 대체한 노드의 자식으로 제거한 노드의 나머지 자식노드를 설정
	- 제거할 노드가 빨간색이었다면 규칙을 어기지 않음
	- 제거할 노드가 검은색인 경우
		- black height가 깨질 확률이 높음
		- 빨간색 노드가 연속으로 올 확률도 높아짐
- 균형을 잡을 때 처리해야할 경우들(제거할 노드가 검은색이었던 경우)
	- 1. 제거할 노드의 형제 노드가 빨간색인 경우
		- 제거할 노드가 제거
		- 형제 노드의 색을 검은색으로 바꿔줌
		- 부모 노드의 색을 빨간색으로 바꿔줌
		- 루트노드에서 터미널 노드인 NIL노드까지 모든 경로에는 같은 수의 검은색 노드가 있어야한다는 규칙을 지키기 위해 회전 필요
		- 부모 노드 왼쪽에 있던 자식을 제거한 경우 부모노드를 왼쪽으로 회전
		- 새로운 형제노드가 생김
		- 부모 노드를 검은색으로 바꿔주고
		- 새로운 형제 노드를 빨간색으로 바꿔줌
	- 2. 형제노드와 형제 노드의 두 자식노드가 모두 검은색이고 부모노드는 빨간색인 경우
		- 제거할 노드 제거
		- 형제 노드의 색을 빨간색으로 바꿔줌
		- 부모노드의 색을 검은색으로 바꿔줌
	- 3. 형제 노드와 형제 노드의 두 자식노드, 부모노드가 모두 검은색인 경우
		- 제거할 노드 제거
		- 형제 노드의 색을 빨간색으로 바꿔줌
		- 이 경우 부모노드와 할아버지 노드 사이에 다른 노드가 있었다고 가정하고 이 노드를 제거한 경우로 가정함
		- 그렇다면 부모노드가 대체할 노드가 되고 할아버지 노드가 부모노드 , 그 여 노드가 형제노드가 됨
		- 이는 두번째 경우와 같으므로 같은 처리를 해줌
		- 새로운 형제노드를 빨간노드, 새로운 부모노드를 검은색 노드로
	- 4. 형제노드가 검은색이고 형제의 두 자식 노드 중 하나라도 빨간색 노드가 있고 바깥쪽 조카노드가 검은색인 경우
		- 바깥쪽 조카노드
			- 형제노드의 자식 노드 중에서 제거할 노드의 반대쪽에 있는 노드
			- 예를 들어 제거할 노드가 부모노드의 왼쪽에 있으면 바깥쪽 조카노드는 형제 노드의 오른쪽 노드
			- 안쪽 조카노드는 제거할 노드와 같은 쪽 노드
		- 제거할 노드를 제거(자리 기억)
		- 부모 노드의 왼쪽 자식노드는 제거할 노드의 자식 노드가 됐음
		- 여기서 안쪽 조카노드를 검은색으로 형제 노드의 색을 빨간색으로 바꿔줌
		- 형제 노드를 제거한 노드의 반대 방향으로 회전시킴
		- 제거할 노드가 부모 노드의 왼쪽에 있었다면 오른쪽으로 회전
		- 이렇게 되면 제거한 노드의 형제노드가 바뀜
		- 새로운 형제노드의 색을 부모노드의 색으로 바꿈
		- 부모노드와 바깥쪽 조카노드의 색을 검은색으로 바꿈
		- 마지막으로 부모노드를 제거한 노드 방향으로 해줌
	- 5. 형제 노드가 검은색이고 형제의 두 자식 노드 중 하나라도 빨간색 노드가 있고 바깥쪽 조카 노드가 빨간색인 경우
		- 제거할 노드 제거함
		- 형제노드의 색을 부모노드의 색으로 바꿔줌
		- 부모노드와 바깥쪽 조카노드의 색을 검은색으로 바꿔줌
		- 제거된 노드 방향으로 부모 노드 회전
## red-black 트리 구현
- 추상 자료형
	- 삽입과 제거에 공통으로 쓰이는 함수
		- rotateLeft(node)
		- rotateRight(node)
		- replaceParentsChild(parent, oldChild, newChild)
			- parent가 자식으로 가리키고 있었던 oldChild를 newChild로 바꿔주는 함수
			- 회전을 하고 나서 자식노드를 바꿔줄 때 사용
	- 삽입
		- insert(data)
		- rebalanceAfterInsertion(node)
			- 데이터 삽입 후 네 가지 경우를 나눠서 recoloring과 회전하는 함수
		- getUncle(parent)
			- 삼촌 노드를 구하는 함수
	- 제거
		- remove(data)
		- removeWithZeroOrOneChild(node)
			- remove함수 내에서 호출되는 함수로 제거할 노드가 1개 이하의 자식 노드를 가지고 있을 때 제거하는 함수
		- getBiggestNode(node)
		- rebalanceAfterDeletion(node)
			- 삭제하고 난 후 균형을 맞춰주는 함수
		- getSibling(node)
			- 균형을 맞추는 동안 형제 노드의 색을 비교해야 하는 경우가 있으므로 형제노드를 구하는 함수 
		- handleRedSibling(node, sibling)
			- 형제노드가 빨간색일 때 균형을 맞춰주는 함수
		- isBlack(node)
			- 검은색 노드인지 구분하는 함수
		- handleBlackSiblingWithAtLeastOneRedChild(node, sibling)
			- 형제 노드가 검은색이고 그 형제노드가 하나 이상의 빨간색 자식 노드를 가지고 있는 경우 균형을 맞춰주는 함수 
- NIL 노드
	- 이 트리에서 모든 터미널 노드는 NIL노드
	- NIL노드는 모드 검은색
	- 구현할 때 null을 nil노드로 생각할 것
	- 예를 들어 현재 노드의 오른쪽 자식 노드가 null이라면 오른쪽 자식 노드는 nil 노드
	- 그러나 이렇게 null을 nil노드로 취급하긴하지만 실제로 노드로 취급하면 오류가 발생
	- null은 노드 속성에 접근할 수 업기 때문(색깔이나 부모노드, 자식노드 참조불가, 함수 호출 불가)
	- 오류를 방지하기 위해 노드의 속성을 가지는 nilnode 생성
	- 노드의 속성이 필요할 때는 Null 대신 nilnode를 만들어 처리하고 처리가 끝나면 다시 null로 바꿔서 nil노드로 표현

## 우선 순위 큐와 힙 - 개념
- 우선순위 큐
	- 큐는 가장 먼저 들어간 데이터가 먼저 나오는 자료구조
	- First In First Out
	- 우선 순위 큐는 데이터가 들어간 순서는 상관이 없고 우선 순위가 높은 데이터가 먼저 나옴
	- 우선 순위는 사용자가 정하기 나름
	- enqueue( )
	- dequeue( )
	- 배열이나 연결리스트는 매번 전체 요소를 정렬해야하고 삽입과 삭제의 성능이 좋지 않기 때문에 '힙'이라는 자료 구조를 이용해 우선순위 큐라는 자료 구조를 만들것
- 힙
	- 힙은 우선 순위 큐의 요구사항에 거의 100프로 일치하기 때문에 우선순위 큐를 구현하기 위한 가장 적절한 자료구조
	- 트리의 일종으로 '완전 이진 트리'
	- 완전 이진트리는 위에서 아래로, 왼쪽에서 오른쪽으로 채워지는 트리
		- 추가적으로 조건이 붙는데 조건에 따라 최소 힙과 최대 힙으로 나눠짐
	- 최소 힙
		- 루트노드가 가장 작은 값을 가지는 완전 이진트리
		- 어느 서브트리를 보더라도 서브트리의 루트노드가 가장 작은 값이어야 함
		- 더 낮은 값이 우선 순위가 높음
	- 최대 힙
		- 루트노드가 가장 큰 값을 가지는 완전 이진트리
		- 어느 서브트리를 보더라도 서브트리의 루트노드가 가장 큰 값이어야 함
		- 더 높은 값이 우선순위가 높음
	- 최소 힙 트리에 노드를 추가할 때 최소 힙 트리의 조건을 위반하게 된다면 삽입한 노드를 부모 노드와 비교해서 우선 순위가 더 높다면 부모노드와 자리를 교체해 나가는 방식으로 조건을 지키도록 함 -> 이 작업을 삽입한 노드의 우선순위가 부모노드보다 더 낮아질 때까지 반복함
	- 삽입
		- 마지막으로 삽입된 노드의 위치를 기억하고 이를 기반으로 계산해서 삽입될 노드의 위치를 찾기
	- 제거 
		- 힙은 우선 순위가 가장 높은 노드를 먼저 제거함 -> 루트 노드 제거
		- 마지막에 삽입된 노드를 루트노드의 위치로 가져옴
		- 두 자식 노드 중 우선순위가 더 높은 노드와 비교해서 현재 노드의 우선순위가 더 낮다면 위치를 바꿔줌
		- 비교를 반복하다가 자식노드의 우선순위가 더 낮으면 제거 종료
		- 제거 과정에서 마지막으로 삽입된 노드의 위치가 지워졌기 때문에 마지막으로 삽입된 노드의 위치를 마지막 이전에 삽입된 노드의 위치로 바꿔줘야 함
	- 힙의 삽입과 제거의 성능은 O(logn)
- 힙을 오름차순으로 정렬
	- 최소 힙에 순서에 상관없이 데이터를 삽입하고
	- 힙이 빌 때까지 제거하면 가장 작은 수부터 제거되기 때문에 오름차순으로 정렬
- 힙을 내림차순으로 정렬
	- 최대 힙에 순서에 상관없이 데이터를 삽입하고 
	- 힙이 빌 때가지 제거하면 가장 큰 수부터 제거되기 때문에 내림차순으로 정렬
## 우선순위 큐와 힙 - 구현 1(힙 삽입)
- getInsertingParent( )
	- 새로운 노드가 삽입될 위치를 구하는 함수
	- 새로운 노드가 삽입될 위치의 부모 노드를 구하는 함수
	- 1. lastInsertedNode가 루트 노드인 경우
		- 이 경우 새로 삽입될 노드의 위치는 루트 노드의 왼쪽 자식
		- 따라서 새로 삽입될 노드의 부모 노드는 루트노드가 됨
	- 2. lastInsertedNode가 부모노드의 왼쪽 자식노드인 경우
		- 새로 삽입될 노드의 위치는 마지막에 삽입된 노드의 오른쪽 형제, 즉 부모노드의 오른쪽 자식
		- 따라서 새로 삽입될 노드의 부모노드는 마지막에 삽입된 노드의 부모노드
	- 3. lastInsertedNode가 부모노드의 오른쪽 자식노드인 경우
		- a. 마지막에 삽입된 노드의 부모노드 중에서 오른쪽 형제 노드를 가지는 노드가 존재하는 경우
			- 먼저 마지막으로 삽입된 노드의 부모 노드 중에서 오른쪽 형제 노드가 있는 노드를 찾고 오른쪽 형제 노드를 참조해서 왼쪽 자식으로 쭉 내려가면 해당 노드가 새로 삽입할 노드의 부모 노드가 됨
		- b. 마지막에 삽입된 노드의 부모 노드중에서 오른쪽 형제 노드를 가지는 노드가 존재하지 않는 경우
			- 이때 새로 삽입될 노드의 부모 노드는 루트노드 부터 시작해서 가장 왼쪽에 존재하는 노드가 됨
- getNewLastInsertedNode( )
	- 가장 마지막에 삽입된 노드의 직전에 삽입된 노드를 구하는 함수
	- 1. 마지막에 삽입된 노드가 부모노드의 왼쪽 자식노드인 경우
		- a. 부모노드 중에서 왼쪽 형제 노드가 존재하는 경우
			- 마지막으로 삽입된 노드의 첫번째 왼쪽 형제노드에서 가장 오른쪽에 있는 자식 노드 선택
		- b. 부모 노드 중에서 왼쪽 형제 노드가 존재하지 않는 경우
			- 루트 노드에서 가장 오른쪽 자식 노드 선택
	- 2. 마지막에 삽입된 노드가 부모노드의 오른쪽 자식 노드인 경우
		- 마지막에 삽입된 노드의 부모노드의 왼쪽 자식 노드 선택
## 트라이와 자동완성 기능 - 개념
- 트라이는 트리의 일종
- 그러나 이진 트리는 아니기 때문에 자식 노드의 개수 제한이 없음
- 트라이는 저장하려는 단어를 한 글자씩 나눠서 해시 테이블을 사용해 key에 글자를, value엔 자식 노드를 저장함
- 루트노드에 자식 노드들 저장
- 자식 노드에는 그 자식의 노드 저장
- 해시테이블의 검색 시간 성능은 O(1)
- 트라이의 성능은 O(k) 
	- k는 트라이에 저장된 노드 수
## 그래프 - 개념
- 그래프는 관계를 표현하기 좋은 자료구조로 데이터가 서로 어떻게 연결되는지를 표현
- 트리는 그래프의 한 종류
- 트리는 그래프라고 말 할 수 있지만 그래프는 트리라고 말할 수 없음
	- '노드는 부모와 자식으로 계층적 구조를 가지며 그래프에서 사이클이 없어야한다'는 조건을 만족하면 트리
- 그래프 용어
	- 각각의 데이터를 나타내는 노드를 그래프에서는 '정점'이라고 부름
	- 정점을 잇는 선을 간선이라고 함
	- 간선으로 연결된 정점은 서로 인접했다고 말하고 인접한 정점은 이웃이라고 함
	- 방향이 있는 그래프를 방향 그래프
## 깊이 우선 탐색 알고리즘 - 개념
- 그래프 탐색은 방문한 정점을 방문했다고 표시함
- 만약 방문한 정점을 기록하지 않는다면 그래프에서 사이클을 이루는 부분이  무한 순회를 하게 됨
- 따라서 방문한 정점을 해시테이블에 저장할 것
	- 해시테이블은 검색의 시간 복잡도가 O(1)로 아주 빠르기 때문
	- 1. 현재 정점을 해시테이블에 저장
	- 2. 현재 정점의 인접 정점들을 순회(방문했던 정점이면 순회하지 않음)
	- 3. 방문하지 않았던 정점이면 그 정점에 대해서 재귀적으로 깊이 우선 탐색을 수행
- Ben을 검색한 경우
	- 깊이 우선 탐색 함수를 실행
	- 콜스택에 ben을 호출한 함수가 push됨
	- 그리고 ben을 방문한 정점으로 해시테이블에 저장
	- 그 후 ben의 인접 정점 방문
	- 이 과정 반복
	- 더 이상 방문할 정점이 없으면 콜스택에서 pop되면서 함수 종료
	- 아래 함수로 실행이 넘어감
	- 방문하지 않은 인접정점이 있으면 다시 탐색
- 즉, 깊이 우선 탐색은 시작 정점의 인접 정점 중 하나를 먼저 끝까지 탐색하고 나머지 인접 정점도 같은 방식으로 탐색하는 알고리즘
## 너비 우선 탐색 알고리즘 - 개념
- 1. 방문한 정점으로 저장하고 방문한 정점을 큐에 삽입
- 2. 큐에서 dequeue
- 3. dequeue한 정점의 인접 정점 순회 
	- 만약 인접 정점이 이미 방문한 정점이라면 건너뀌고 방문하지 않은 정점이라면 첫 번째 경우 반복
	- 인접한 정점을 방문하고 방문한 정점으로 해시테이블에 저장하고 큐에 넣음
- 4. 큐가 빌 때까지 반복하면 모든 정점을 탐색하는 너비 우선 탐색 완료
- 큐가 비면 탐색이 완료된 것, 방문한 정점을 저장하는 해시테이블에 정점들이 저장됨
- 시작 정점을 기준으로 넓게 퍼지게 검색한다고해서 너비 우선 탐색

## 깊이 우선 탐색 VS 너비 우선 탐색
- 깊이 우선 탐색
	- 시작 정점의 인접 정점을 방문하면 그 정점의 인접 정점을 끝까지 탐색하는 기법
	- 정점을 깊이 파고 들면서 탐색하기 때문에 깊이 우선 탐색이라는 이름이 붙음
- 너비 우선 탐색
	- 깊이 우선 탐색처럼 시작 정점에서 인접 정점 탐색
	- 그러나 인접 정점의 인접 정점을 방문하는 것이 아니라 시작 정점의 다른 인접 정점들을 차례대로 방문
	- 너비 우선 탐색은 이렇게 방문한 정점의 모든 인접 정점을 먼저 탐색하는 기법
	- 정점을 넓게 탐색하기 때문에 너비 우선 탐색이라는 이름이 붙음
- 둘은 상황에 따라 더 적합한 경우가 다르기 때문에 상황에 맞춰 사용
	- 깊이 우선 탐색을 사용하면 지인의 지인 이런식으로 친구가 아닐 확률
	- 너비 우선 탐색은 친구
	- 자손을 찾는 경우는 깊이 우선 탐색이 더 빠르게 자손에 도달
- 성능
	- 그래프의 성능은 정점뿐아니라 간선에 따라서도 달리짐
	- O( V + E )
		- V는 정점의 수, E는 간선의 수
## 가중 그래프
- 기존의 그래프를 약간 변형한 그래프
	- 간선의 크기를 저장하는 것
	- 크기는 정점 간의 거리가 될 수도 있고, 정점 간의 비용이 될 수도 있음
- 가중 그래프는 방향도 가질 수 있음
## 다익스트라 알고리즘 - 개념
- 가장 빠른 경로를 찾는 최단 경로 알고리즘
- 각 정점까지 거리 정보를 저장하는 가중 그래프를 이용해 최단 경로를 계산하는 알고리즘
- 어떤 경로를 거쳐 최단 거리가 되는지 역시 알 수 있음
## 탐욕 알고리즘
- 최적해를 구하는데 사용되는 근사적인 방법
- 매 선택지에서 현재 상황에서 가장 좋다고 판단하는 선택을 하는 것
- 탐욕 알고리즘이 항상 최적해를 찾는 것은 아님
- 최적해를 얻을 수 있는 조건
	- 1. 탐욕스러운 선택 속성
		- 앞의 선택이 이후의 선택에 영향을 주지 않는다는 뚯
	- 2. 최적 부분 구조 조건
		- 문제에 대한 최적해가 부분 문제에 대해서도 역시 최적해라는 것
		- 문제에 대한 최적해가 부분 문제의 최적해와 같지 않으면 최적 부분 구조 저건을 가지고 있지 않은 상황임
	- 위 두조건을 만족하면 탐욕 알고리즘을 통해 항상 최적해를 찾을 수 있음
		- 이러한 구조를 '매트로이드'라고함
		- 자료구조를 바꿔서 매트로이드로 만들어 최적해를 구하거나 매트로이드로 만들 수 없다면 근사해를 얻는 방법으로 사용할 수 있음
- 동전 교환 문제
	- 500원, 100원, 50원, 10원 중에서 500원을 최대한 많이 줘서 동전 수를 가장 적게 거슬러 줄 수 있음
	- 각 동전은 서로 배수와 약수 관계 -> 매트로이드 문제가 됨
## 최소 신장 트리 - 개념
- 그래프에서 순환 구조가 발생하지 않는 것을 트리
- 신장 트리
	- 그래프에 순환 구조가 없이 모든 정점이 연결되어있는 트리
- 최소 신장 트리
	- 수많은 신장트리에서 간선의 길이가 가장 짧게 연결된 트리
- 1. 크루스칼 알고리즘
	- 
- 2. 프림 알고리즘
	- 최단 경로 알고리즘인 다익스트라 알고리즘과 비슷
	- 매 순간 가장 가까운 정점을 찾아서 모든 정점을 가장 짧게 연결하는 알고리즘으로 탐욕 알고리즘에 속함
- 프림 알고리즘 구현
	- 임의의 시작 도시 선택 -> 예시에서는 서울로 선택
	- 방문한 도시와 방문하지 않은 도시로 나눔
	- 처음엔 모든 도시를 방문하지 않았기 때문에 방문한 도시 배열은 비어있고 방문하지 않은 도시 배열에는 모든 도시가 저장되어 있음
	- 도시의 연결을 나타내는 mst_table

	- 제일 먼저 서울에서 서울은 연결할 필요가 없으므로 0으로 초기화
	- 방문하지 않은 도시가 없을 때까지 아래 작업 반복
	- 방문하지 않은 도시 중 mst_table에서 거리가 가장 가까운 도시를 방문
	- 지금은 모든 도시를 방문하지 않았고 mst_table에는 서울의 거리가 0으로 가장 가깝기 떄문에 서울을 방문
	- 방문한 도시에 서울을 추가하고 방문하지 않은 도시에서 서울 제거
	- 이제 방문한 도시의(서울) 방문하지 않은 인접도시 순회
	- 서울의 인접도시는 원주 강릉 대전 전주
	- 먼저 원주를 순회한다고 가정
		- 서울에서 원주까지 거리는 87km
		- mst_table에서 원주까지 거리는 무한대로 설정되어있는 상태
		- 87이 무한대보다 더 작기 때문에 mst_table에서 원주를 87로, 이전 도시는 서울로 저장
	- 다음으로 서울의 다음 인접도시인 강릉 순회
		- 서울에서 강릉까지 거리는 165km
		- mst_table에서 강릉까지 거리는 무한대로 설정되어있는 상태
		- 165이 무한대보다 더 작기 때문에 mst_table에서 강릉을 165로, 이전 도시는 서울로 저장
	- 이 과정을 대전과 전주도 반복
	- 서울의 인접도시를 모두 순회했으나 현재는 서울만 방문했고 나머지 도시는 방문하지 않은 상태이므로 방금 작업 다시 반복
	- 방문하지 않은 도시 중 mst_table에서 거리가 가장 가까운 도시는 원주
	- 방문한 도시에 원주를 추가하고 방문하지 않은 도시에서 원주 제거
	- 이제 방문한 도시(원주)의 방문하지 않은 인접 도시 순회
	- 원주의 인접도시는 서울 강릉 대전 대구
		- 서울은 이전에 방문했기 때문에 순회하지 않음
		- 먼저 강릉 순회
			- 원주에서 강릉까지 거리는 95
			- mst_table을 보면 강릉까지 거리는 165
			- 95가 165보다 더 작기 때문에 mst_table에서 강릉을 95로 이전 도시는 원주로 저장
		- 다음 인접 도시 대전 순회
			- 원주에서 대전까지 거리는 118
			- mst_table을 보면 대전은 140
			- 118이 140보다 작기때문에 mst_table에서 대전을 118로 이전 도시는 원주로 저장
		- 다음 인접도시 대구를 순회함
		- 이 과정들을 방문하지 않은 도시 배열이 빌때가지 반복하면 됨
		- mst_table을 바탕으로 이전 도시 정보로 도시들을 연결 
## 최대 유량 문제 - 개념
- 최대 유량 문제는 용량이 다른 여러 상수관을 통해서 source(시작점)에서 sink(도착점)까지 최대한 많은 양의 물을 보내는 문제
- 한 상수관에 최대로 흐를 수 있는 물의 양을 용량(capacity)
- 현재 상수관에 흐르고 있는 물의 양은 유량(flow)
- 상수관의 용량에서 유량을 빼면 추가로 흐를 수 있는 물의 양을 나타내는데 이것을 잔여 용량이라고 함
- 필연적으로 최대 유량을 찾는 알고리즘은 포드 풀커슨 알고리즘/ 에드몬드 카프 알고리줌

- 포드 풀커슨 알고리줌
	- 탐욕 알고리즘으로 그 순간 최선의 선택을 하는 알고리즘
	- 포드 풀커슨 알고리즘은 깊이 우선 탐색으로 경로를 찾고 에드몬드 카프 알고리즘은 너비 우선 탐색으로 경로를 찾음
	- 1. source에서 sink로 가는 경로 하나를 깊이 우선 탐색으로 찾는다.
		- 증가 경로
	- 2. 증가 경로에 흐를 수 있는 최대 유량을 찾는다.
		- 보낼 수 있는 최대 유량은 증가 경로에 있는 상수관에서 가장 적게 남은 잔여 용량 만큼임
		- 다른 상수관의 잔여 용량이 아무리 많이 남았어도 한곳의 잔여 용량이 적다면 그만큼만 흘려보낼 수 있기 때문
		- 잔여 용량 = 용량 - 유량
	- 3. 찾은 증가 경로에서 보낼 수 있는 최대 유량을 실제로 흘려 보냄
	- 위 세 단계를 증가 경로를 찾을 수 없을 때까지 반복하면 최대 유량을 보낼 수 있는 경로를 찾을 수 있음
## 인터벌 스케쥴링
- 최대한 많은 세션을 들을 수 있는 선택을 찾아라 라고 질문하는 문제
- 탐욕 알고리즘에서 많이 쓰이는 예제
- 1. 겹치는 세션의 수가 가장 적은 것부터 선택
- 2. 세션의 시간이 가장 짧은 것부터 선택
- 3. 시작 시각이 가장 이른 세션부터 선택
- 4. 종료 시각이 가장 이른 세션부터 선택
	-  이 중 네번째 방법으로 최적해를 얻을 수 있음
## 비트 마스킹
- 비트
	- 컴퓨터가 다루는 최소 단위의 데이터
	- 0이나 1, 두 가지 숫자만 표현할 수 있음
	- 8비트는 1바이트
	- 변수의 자료형
		- 정수는 int로 표현함
		- int형 변수는 32비트
		- 따라서 4바이트 자료형이라고 부름
- 비트 연산(진리표)
	- 가장 기본적인 비트 연산
	- AND 연산
		- AND연산은 두 입력이 모두 참(1)인 경우에만 출력이 1
		- &(앰퍼샌드)
			- 1001과 0001을 &로 AND연산하면 각 자릿수끼리 연산 진행
			- 결과적으로 0001이 출력되면서 10진수 1이 출력됨
			- AND연산의 출력은 두 입력을 곱한 결과와 같다고 해서 논리곱이라고도 불림
	- OR 연산
		- 입력 값 중 하나라도 참(1)이면 출력값이 1
		- 자바스크립트에서 OR연산은 수직바( | )로 함
			- 1001과 0110을 OR연산
			- 결과적으로 1111이 출력되면서 10진수 15가 출력됨
			- OR연산의 출력은 두 입력을 더한 결과와 같다고해서 논리합이라고도 불림
	- XOR 연산
		- 두 입력이 서로 다르면 1
		- 입력에서 1의 개수가 홀수이면 출력이 1이다.
		- 자바스크립트에서 XOR은 캐럿 ( ^ )로 함
			- 1001과 1111을 XOR 연산
			- 0110이 출력되면서 10진수로 6이 출력됨
	- 비트 시프트 연산자
		- 0110으로 비트가 있다면 래스댄 사인 두개( << )로 왼쪽 시프트 연산을 할 수 있음
		- 0110 << 1
		- 모든 비트들이 왼쪽으로 1칸 이동
		- 자릿수를 넘어가면 버려지고 오른쪽은 0으로 채워짐
		- 0110은 1100이 되어서 10진수 12가 됨
		- 만약 래스댄 사인 뒤에 1이 아닌 2를 넣어주면 왼쪽으로 두 칸 이동함
	- 비트 마스크
		- 비트 연산을 이용해서 본인이 원하는 비트를 컨트롤 할 수 있는 방법
		- 원하는 자릿수에 비트를 쓰거나 읽는 용도로 사용함
## 외판원 문제 - 개념
- 외판원이 각각 연결되어 있는 도시들을 전부 방문하려고 함
- 연결된 경로엔 이동 비용이 적혀있음
- 외판원이 있는 도시에서 모든 도시를 한 번씩만 방문하고 다시 출발 도시로 오는 최소 비용의 경로를 구하는 문제
- 최소 비용의 경로를 구하려면 모든 경로를 전부 구해서 그 중에 최소 비용인 것을 선택해야 함
- 도시가 늘어날수록 갈 수 있는 경로가 팩토리얼로 늘어남
	- 도시가 3개일 때 2!
	- 도시가 4개일 때 3!
	- 도시가 5개일 때 4!
		- 팩토리얼을 빅오로 정의하면 O(n의 n승) 즉 지수시간
- 동적 프로그래밍을 이용하면 이보다는 개선된 성능으로 외판원 문제를 해결할 수 있음
- 외판원 문제 알고리즘(동적 프로그래밍)
	- 4개의 도시가 있다고 가정, 편의상 간선의 방향은 없음
	- 이럴 때 각 도시의 연결을 2차원 배열로 표현할 수 있음
	- 배열의 크기는 (도시의 수) X (도시의 수)
	- 4 X 4 테이블 생성
	- 첫 번째 행은 1번도시의 간선
		- 첫 번째 열은 자기 자신으로 경로가 없다는 뜻이므로 0
		- 두 번째 열은 1에서 2로 가는 비용으로 4
		- 세 번째 열은 1에서 3으로 가는 비용으로 1이고,
		- 네 번째 열은 1에서 4로 가는 비용으로 3
	- 마찬가지로 두 번째 행은 2번도시의 간선
		- 위와 같이 열의 정보 나타낼 수 있음
	- T(1,{2,3,4})
		- 이 식은 출발 도시 1에서 방문하지 않은 도시 2,3,4를 방문하는 최소 비용을 말함
		- 즉, 도시 1에서 출발해서 2, 3,4에서 나올 수 있는 최소 경로를 구하는 식
		- 이 식을 재귀적으로 표현할 수 있음
			- (1,2) + T(2,{3,4})
				- 1에서 2로 가는 걸 선택했을 때 1에서 2로가는 비용 + 출발도시 2에서 방문하지 않은 도시 3,4를 방문하는 최소 비용
			- (1,3) + T(3,{2,4})
				- 1에서 3으로 가는 걸 선택했을 때 1에서 3으로가는 비용 + 출발 도시 3에서 방문하지 않은 도시 2,4를 방문하는 최소비용
			- (1,4) + T(4,{2,3})
				- 1에서 4로 가는 걸 선택했을 때 1에서 4로가는 비용 + 출발 도시 4에서 방문하지 않은 도시 2,3을 방문하는 최소비용
		- 이렇게 재귀적으로 표현한 식은 다시 재귀적으로 표현할 수 있음
			- 예를 들어 (1,2) + T(2,{3,4})
			- T(2,{3,4})를 (2,3) + T(3,{4}) 와 (2,4) + T(4,{3})로
			- T(3,{4})를 (3,4) + T(4, {})
		- 문제를 더 이상 쪼갤 수 없을만큼 쪼갰다면 답이 구해짐
		- 

















	


















	



















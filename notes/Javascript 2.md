- 사용자와 상호작용이 가능하게 해주는 언어
- 동적으로 작동하게 해줌
1. body 태그의 back ground color 변경 
2. font color 변경
- html의 onclick=""의 속성란에는 무조건 자바스크립트가 들어오도록
  -> 예를 들면document.querySelector('body').style.backgroundColor='black';
- script 안 속성란도 자바로 해석
- =은 대입연산자
- x와 같은 변수는 대입연산자를 통해 값이 바뀔 수 있지만 1과 같은 숫자는 불가능
- name =''; 변수 지정
	- 글 시작 전에 name 변수 지정하고 글 중간에 "+name+"하면 name 값에 따라 글의 내용이 바뀜
	- 변수를 사용할 때는 앞에 var을 붙여주는 것이 좋다 (var name="") 이런식
- HTML에서 1&lt;2 의 의미는 JAVA의 1<2

## 모든 selector 선택
- var alist= document.querySelectorALL('a');
	- 모든 a 선택자 값의 모음을 alist라는 변수로 지정

## 반복문
- while( ){ }
	- ( )안에는 언제까지 반복할 것인지 
	 { }안에는 무엇을 반복해서 실행 할 것인지
- while 반복문 맨 위에 var i 로 i 변수를 지정하고 반복문 안에 i=i+1을 하면 반복하면서 i값이 하나씩 더해지는 효과
- while(i<alist.length){console.log(alist[i]);
- 반복문은 java 파일의 6.html 참고

## 함수
```
function name (parameter1, parameter2,...) {
	// Execution statement
}
```
- 코드를 재사용할 수 있다. (코드를 한 번 정의하고, 여러 번 사용)
- 다른 인자를 사용하여 동일한 코드를 여러 번 사용할 수 있으며, 다른 결과를 도출할 수 있음
- 변수안에 담길 수 있다. (함수가 값이기 때문)
- 객체의 속성안에 method로 담길 수 있다.
- 다른 함수의 인자값을 전달될 수 있다.
- 함수의 return값으로도 사용할 수 있다.
- 배열의 값으로도 사용할 수 있다.

- 변수를 선언하고 함수를 대입하는 방식을 함수 표현식이라고 한다.

```null
var addOne = function(x) {
  var y = x + 1;
  return y;
};
```

- name은 함수의 이름이고 뒤에는 함수의 이름으로 설정된 단어가 어떤 역할을 수행하는지를 프로그래밍하는 것

## 객체
```null
var user = {
  name: "Surim Son",
  age: 22
};
```

- **객체는 변수이다. 그러나 객체에는 많은 값이 포함될 수 있다.**  
    (자바스크립트 변수처럼 단일 값을 포함 할 수 있다.)
- 객체는 중괄호 표기를 이용하여 만들 수 있다.
- 객체는 각각의 key/value에 대한 정보를 나열할 수 있다.
- Key는 문자열 또는 기호여야 한다.
- Value는 모든 유형이 될 수 있다.
- 객체는 한 쌍의 key/value 뒤에 쉼표를 이용하여 그 뒤에 오는 key/value와 구분해주어야 한다.
- 객체에서 명명된 값을 Properties라고 한다.
- 변수(위에서 user에 해당)는 예약어의 이름을 가질 수 없지만 객체는 어떠한 이름이어도 상관없다.
- 객체 변수를 복사하면 참조가 복사되고 객체가 복제되지 않는다.
## 배열
- 자바스크립트는 명시적인 배열 자료형이 없고 자바스크립트 객체의 일종으로 취급
- 배열 요소에 어떤 타입의 데이터형도 가능
- 동적으로 크기 조절도 가능
- **요소가 순서대로 저장**
- 배열은 순서로써 접근 가능, 객체는 순서없이 키로 접근
-  var arr = [1,2,3,4,5];
- 

## UI VS API
- UI: 사용자가 사용하기 위해서 조작하는 장치
- API: 작동하기 위한 기능을 만들어 두는 조작 장치
	 모든 애플리케이션들은 api를 통해 순서대로 만들어짐


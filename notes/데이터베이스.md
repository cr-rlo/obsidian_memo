## database(DB)
- 전자적으로 저장되고 사용되는 관련있는 데이터들의 조직화된 집합
- 관련있는 데이터
	- 예를 들어 로그인하기 위해서 입력한 개인정보
	- 같은 출처, 같은 서비스 등에서 생산되는 데이터들
- 조직화된 집합
	- 찾으려는 데이터들을 더 빨리 찾을 수 있게 해줌
	- 불필요한 데이터가 중복해서 생성되는 것도 막아줌
	- 데이터의 불일치도 막아줌
- 전자적으로 저장되고 사용
	- 컴퓨터에 저장되고 사용
## DBMS
- 사용자에게 DB를 정의하고 만들고 관리하는 기능을 제공하는 소프트웨어 시스템
- DB를 정의하다보면 부가적인 데이터가 발생함 -> 메타 데이터
## metadata
- database를 정의하거나 기술하는 data
- catalog라고도 부름
- 데이터 유형, 구조, 제약 조건, 보안, 저장, 인덱스, 사용자 그룹 등
- metadata 또한 DBMS를 통해 저장/ 관리
## database system
- database + DBMS + 연관된 applications
- 줄여서 database라고도 부름
## data models
- DB의 구조를 기술하는데 사용될 수 있는 개념들이 모인 집합
	- DB구조: 데이터 유형, 데이터 관계, 제약사항 등
- DB구조를 추상화해서 표현할 수 있는 수단을 제공
- data model은 여러 종류가 있으며 추상화 수준과 DB 구조화 방식이 조금씩 다름
- DB에서 읽고 쓰기 위한 기본적인 동작들도 포함됨
- data model 분류
	- conceptual (or high level) data models
		- 일반 사용자들이 쉽게 이해할 수 있는 개념들로 이뤄진 모델
		- 추상화 수준이 가장 높음
		- 비즈니스 요구 사항을 추상화하여 기술할 때 사용
		- entity-relationship model(ER diagram)
			- entity 사각형
			- 관계 마름모
			- 속성(attribute) 타원형
	- logical (or representational) data models
		- 이해하기 어렵지 않으면서도 디테일하게 DB를 구조화 할 수 있는 개념을 제공
		- 데이터가 컴퓨터에 저장될 때의 구조와 크게 다르지 않게 DB 구조화를 가능하게 함
		- 특정 DBMS나 storage에 종속되지 않는 수준에서 DB를 구조화할 수 있는 모델
		- relational data model
			- 오라클, mysql, sql server
		- object data model
		- object-relational data model
	- physical (or low level) data models
		- 컴퓨터에 데이터가 어떻게 파일 형태로 저장되는지를 기술할 수 있는 수단을 제공
		- data format, data orderings, access path등등
			- access path: 데이터 검색을 빠르게 하기 위한 구조체(index등)
## 데이터 베이스의 schema & state
- database schema
	- data model을 바탕으로 database의 구조를 기술한 것
	- schema는 database를 설계할 때 정해지며 한 번 정해진 후에는 자주 바뀌지 않음
	- 데이터베이스가 어떤 구조를 갖고 있는지 큰 그림을 알 수 있음
- database state
	- database에 있는 실제 데이터는 꽤 자주 바뀔 수 있음
	- 특정 시점에 database에 있는 데이터를 database state 혹은 snapshot이라고 함
	- 혹은 database에 있는 현재 instances의 집합이라고 함
- three-schema architecture
	- database system을 구축하는 architecture 중 하나
	- user application으로 부터 물리적인 database를 분리시키는 목적
		- 물리적인 데이터베이스의 구조가 바뀔 수 있는데 이때 user application에 영향을 미치지 않게 하려고 분리
		- 각 레벨을 독립시켜서 어느 레벨에서의 변화가 상위 레벨에 영향을 주지 않기 위함
	- 세가지 level이 존재하며 각각의 level마다 schema가 정의되어 있음
		- external schemas at external level
			- external views, user views 라고도 불림
			- 특정 유저들이 필요로 하는 데이터만 표현
			- 그 외 알려줄 필요가 없는 데이터는 숨김
			- logical data model을 통해 표현
			- 실제 사용자가 바라보는 스키마
		- conceptual schema at conceptual level
			- 전체 데이터베이스에 대한 구조를 기술
			- internal schema를 한 번 추상화 시켜서 기술
			- 물리적인 저장 구조에 관한 내용은 숨김
			- entities, data types, relationships, user operations, constraints에 집중
			- logical data model을 통해 기술
		- internal schemas at internal level
			- 물리적인 저장장치에 가장 가깝게 있는 스키마
			- 물리적으로 데이터가 어떻게 저장되는지 physical data model을 통해 표현
			- data storage, data structure, access path 등등 실체가 있는 내용 기술
			- 데이터가 존재하는 곳
## data definition language(DDL)
- conceptual schema를 정의하기 위해 사용되는 언어
- internal schema 까지 정의할 수 있는 경우도 있음
## storage definition language(SDL)
- internal schema를 정의하는 용도로 사용되는 언어
- 요즘은 특히 relational DBMS에서는 SDL이 거의 없고 파라미터 등의 설정으로 대체됨
## view definition language(VDL)
- external schemas를 정의하기 위해 사용되는 언어
- 대부분의 DBMS에서는 DDL이 VDL의 역할까지 수행
## data manipulate language(DML)
- database에 있는 data를 활용하기 위한 언어
- data 추가, 삭제, 수정, 검색 등등의 기능을 제공하는 언어
## 관계형 데이터베이스(relational data model)
- set
	- 서로 다른 elements 를 가지는 collection
	- 하나의 set에서의 elements의 순서는 중요하지 않다.
	- set A에서 엘리먼트 하나를 고르고 set B에서 엘리먼트를 하나를 골라서 A와 B로 만들 수 있는 모든 페어의 수 => A * B (cartesian product)
- binary relation
	- set이 2개인 경우
	- A와 B의 cartesian product를 한 페어들의 부분집합
- n-ary relation
	- set이 n개 일 때 cartesian product
	- x1 * x2 * x3 * .... xn
	- 이렇게 n개의 엘리먼트들로 이뤄진 각각의 리스트를 튜플이라고 함
- relation in mathematics, relational data model
	- relational data model에서 이 set은 도메인을 의미
	- 도메인은 엘리먼트 혹은 밸루 라고 하는데, 이런 값들의 집합을 도메인이라고 함
	- 도메인마다 이름을 붙일 수도 있음
	- 릴레이션 모델에서도 마찬가지로 튜플들을 가질 수 있음
## relational data model 이해하기
- student relation을 예로 들기
	- domain 정의하기
		- students_ids: 학번 집합, 7자리 integer 정수
		- human_names: 사람이름 집합, 문자열
		- university_grades: 대학교 학년 집합, {1,2,3,4}
		- major_names: 대학교에서 배우는 전공 이름 집합
		- phone_numbers: 핸드폰 번호 집합
			-  비상연락망을 위해 같은 도메인이 2번사용 그러나 목적이 다른 경우
			- 그러한 역할을 표시해주기 위해 attribute
- 이 튜플들을 테이블로 표현하기 좋기 때문에 테이블로 표시
- <mark style="background: #FFB86CA6;">주요 개념</mark>
	- domain: set of atomic values
	- domain name: domain 이름
	- attribute: domain이 relation에서 맡은 역할 이름
	- tuple: 각 attribute의 값으로 이뤄진 리스트, 일부 값은 null일 수 있음
	- relation: set of tuples
	- relation name: relation의 이름
## relation schema
- relation의 구조
- relation 이름과 attributes 리스트로 표기됨
- attributes와 관련된 constraints도 포함
- ex) student(id, name, grade, major, phone_num, emer_num)
- degree of relation
	- relation schema에서 attributes의 수
	- ex) student(id, name, grade, major, phone_num, emer_num) => degree 6
## relation의 특징들
- relation은 중복된 튜플을 가질 수 없다.
- relation의 튜플을 식별하기 위해 attribute의 부분집합을 key로 설정한다.
- relation에서 tuple의 순서는 중요하지 않다.
- 하나의 relation에서 attribute의 이름은 중복되면 안된다.
- attribute는 atomic해야 한다.
	- composite , multivalued attribute는 잘라줘야함, 나눠줘야함
	- 원자성을 가지게 해야함
## keys
- super key
	- relation에서 tuples를 unique하게 식별할 수 있는 attributes 'set'
- candidate key
	- 어느 한 attribute라도 제거하면 unique하게 tuples를 식별할 수 없는 super key
	- key or minimal super key
- primary key
	- relation에서 tuples를 unique하게 식별하기 위해 선택된 candidate key
- unique key
	- primary key가 아닌 candidate key
	- alternate key
- foreign key
	- 다른 relation의 pk를 참조하는 attributes set
## constraints
- relation database의 relations들이 언제나 항상 지켜줘야 하는 제약 사항
- implicit constraints
	- relational data model 자체가 가지는 constraints
	- relation은 중복되는 tuple을 가질 수 없다
	- relation 내에서는 같은 이름의 attribute를 가질 수 없다.
## schema-based constraints
- 주로 DDL을 통해 schema에 직접 명시할 수 있는 constraints
- explicit constraints
- domain constraints
	- attribute의 value는 해당 attribute의 domain에 속한 value여야 한다.
- key constraints
	- 서로 다른 tuples 는 같은 value의 key를 가질 수 없다.
- NULL value constraints
	- attribute가 NOT NULL로 명시됐다면 NULL을 값으로 가질 수 없다.
- entity integrity constraints
	- primary key는 value에 NULL을 가질 수 없다.
- referential integrity constraints
	- FK와 PK와 도메인이 같아야하고 PK에 없는 values를 FK가 값으로 가질 수 없다.
## 데이터베이스 생성하기
- SQL의 뜻
	- structured query language
	- 현업에서 쓰이는 relational DBMS의 표준 언어
	- 종합적인 database 언어: DDL + DML + VDL
- SQL 주요 용어
	- relational data model VS SQL
		- relation VS table
		- attributes VS column
		- tuple VS row
		- domain VS domain
- SQL에서 relation이란?(특이점)
	- multiset of tuples
	- 중복된 tuple을 허용한다.
- DATABASE VS SCHEMA
	- MYSQL에서는 둘이 같은 뜻을 의미함
	- 다른 RDBMS에서는 의미가 다르게 쓰임
## 예제를 통해 SQL로 DB 정의하기
- IT 회사 관련 RDB 만들기
	- 부서 사원 프로젝트 관련 정보들을 저장할 수 있는 관계형 데이터 베이스 만들기
	- 사용할 RDBMS는 MySQL
- database 정의하기
	- SHOW DATABASES;
		- 현재 데이터베이스들이 뭐가 있는지 보여줌
	- CREATE DATABASE company;
		- company라는 이름의 데이터베이스 생성
	- SELECT database();
		- 지금 선택된 데이터베이스를 알고싶을 때
	- USE company;
		- 사용할 데이터베이스 선택
	- DROP DATABASE company;
		- company 데이터베이스 지우기
- table 만들기
	- DEPARTMENT(id, name, leader_id)
	- EMPLOYEE(id, name, birth_date, sex. position, salary, dept_id)
	- PROJECT(id, name, leader_id, start_date, end_date)
	- WORKS_ON(empl_id, proj_id)
- create table DEPARTMENT
	- (id INT PRIMARY KEY, 
	- name VARCHAR(20) NOT NULL UNIQUE, 
	- leader_id INT);
- 
## attribute data type: 숫자
- 정수
	- 정수를 저장할 때 사용
	- TINYINT
	- SMALLINT
	- MEDIUMINT
	- INT or INTEGER
	- BIGINT
- 부동 소수점 방식
	- 실수를 저장할 때 사용
	- 고정 소수점 방식에 비해 정확하지 않음
	- FLOAT
	- DOUBLE or DOUBLE PRECISION
- 고정 소수점 방식
	- 실수를 정확하게 저장할 때 사용
	- DECIMAL or NUMERIC
	- DECIMAL을 사용할 때는 DECIMAL(precision, scale)
		- precision은 몇 자리 수인지, scale은 소수점 아래 몇 자리 인지
## attribute data type: 문자열
- 고정 크기 문자열
	- 최대 몇 개의 문자를 가지는 문자열을 저장할지를 지정
	- 저장될 문자열의 길이가 최대 길이보다 작으면 나머지를 space로 채워서 저장
	- name char(4)일 때 다음과 같이 저장: 'a___' 
	- CHAR(n)
- 가변 크기 문자열
	- 최대 몇 개의 문자를 가지는 문자열을 저장할지를 지정
	- 저장될 문자열의 길이 만큼만 저장
	- name varchar(4)일 때 다음과 같이 저장: 'a'
	- VARCHAR(n)
- 사이즈가 큰 문자열
	- 사이즈가 큰 문자열을 저장할 때 사용
	- TINYTEXT
	  TEXT
	  MEDIUMTEXT
	  LONGTEXT
## attribute data type: 날짜와 시간
- 날짜
	- 년, 월, 일을 저장
	- DATE
- 시간
	- 시, 분, 초를 저장
	- TIME
- 날짜와 시간
	- 날짜와 시간을 같이 표현
	- TIMESTAMP는 time-zone이 반영됨
	- DATETIME
	- TIMESTAMP
## attribute data type: 그 외
- byte-string
	- 문자열이 아니라 byte string을 저장
	- BINARY
	- VARBINARY
	- BLOB type
- boolean
	- true/ false를 저장
	- MYSQL에는 따로 없음 -> TINYINT로 대체해서 사용
- 위치 관련
	- 위치 관련 정보를 저장
	- GEOMETRY
	- etc
- JSON
	- json 형태의 데이터를 저장
	- JSON

## key constraints: PRIMARY KEY
- table의 tuple을 식별하기 위해 사용, 하나 이상의 attributes로 구성
- primary key는 중복된 값을 가질 수 없으며, NULL도 값으로 가질 수 없다.
- primary key 선언하는 방법
	- attribute 하나로 구성될 때
		- create table PLAYER(id, INT, PRIMARY KEY ...);
	- attribute 하나 이상으로 구성될 때
		- create table PLAYER(team_id VARCAHR(12), back_number INT, ... PRIMARY KEY(team_id, back_number));
## key constraints: UNIQUE
- UNIQUE로 지정된 attributes는 중복된 값을 가질 수 없다.
- 단, NULL은 중복을 허용할 수도 있다.
- UNIQUE를 선언하는 방법도 PRIMARY KEY 선언과 같다.
## NOT NULL constraints
- attribute가 NOT NULL로 지정되면 해당 attribute는 NULL을 값으로 가질 수 없다.
- create table student(phone_number INT NOT NULL UNIQUE, ...);
## attribute DEFAULT
- attribute의 default 값을 정의할 때 사용
- 새로운 tuple을 저장할 때 해당 attribute에 대한 값이 없다면 default 값으로 저장
- create table Orders(... menu VARCAHR(15) DEFAULT '짜장면', ...);
## CHECK constraint
- attribute의 값을 제한하고 싶을 때 사용
- check를 선언하는 방법
	- attribute 하나로 구성될 때
		- create table EMPLOYEE(.. age INT CHECK(age >= 20));
	- attribute 하나 이상으로 구성될 때
		- create table PROJECT(start_date DATE, end_date DATE, ... CHECK(start_date < end_date));
## referential integrity constraint: FOREIGN KEY
- attribute가 다른 table의 primary key나 unique key를 참조할 때 사용
- create table Employee(... dept_id INT, FOREIGN KEY(dept_id) references DEPARTMENT(id) <mark style="background: #FFB86CA6;">o</mark><mark style="background: #FFB86CA6;">n delete reference_option on update reference_option</mark>)
- reference_option
	- CASCADE: 참조값의 삭제/ 변경 그대로 반영
	- SET NULL: 참조값이 삭제/ 변경 시 NULL로 변경
	- RESTRICT: 참조값이 삭제/ 변경되는 것을 금지
	- NO ACTION: RESTRICT와 유사
	- SET DEFAULT: 참조값이 삭제/ 변경 시 default값으로 변경
## constraint 이름 명시하기
- 이름을 붙이면 어떤 constraint를 위반했는지 쉽게 파악할 수 있다.
- constraint를 삭제하고 싶을 때 해당 이름으로 삭제 가능
- create table EMPLOYEE(.. age INT <mark style="background: #FFB86CA6;">CONSTRAINT age_over_20 </mark>CHECK(age >= 20));
## ALTER TABLE
- table의 schema를 변경하고 싶을 때 사용
- attribute 추가 
	- ALTER TABLE employee ADD blood VARCHAR(2);
- attribute 이름 변경
	- ALTER TABLE employee RENAME COLUMN phone TO phone_num;
- attribute 타입 변경
	- ALTER TABLE employee MODIFY COLUMN blood CHAR(2);
- table 이름 변경
	- ALTER TABLE logs RENAME TO backend_logs;
- primary key 추가
	- ALTER TABLE log ADD PRIMARY KEY(id);
- foreign key 추가
	- ALTER TABLE department ADD FOREIGN KEY(leader_id) REFERENCES employee(id) on update CASCADE on delete SET NULL:
## DROP TABLE
- table을 삭제할 때 사용
- DROP TABLE table_name;
## SQL로 테이블에 데이터를 추가, 수정, 삭제
- 데이터 추가하기
	- <mark style="background: #FFB86CA6;">INSERT INTO </mark>테이블 명<mark style="background: #FFB86CA6;"> VALUES </mark>(1,'MESSI', '1987-02-01', 'M', 'DEV_BACK', 100000000, null);
	- <mark style="background: #FFB86CA6;">INSERT INTO</mark> 테이블 명 (name, birth-date, sex, position, id) <mark style="background: #FFB86CA6;">VALUES</mark> ('JENNY', '2000-10-12', 'F', 'DEV_BACK', 3);
		- 값을 넣는 순서에 자유도가 생김
		- 넣고 싶은 attribute만 입력할 수 도 있음
- SHOW CREATE TABLE 테이블명;
	- 에러 메세지의 명을 알 수 있음
- SELECT * FROM 테이블 명;
	- 전체 attribute에 대한 값을 가져옴
- 데이터 수정하기
	- <mark style="background: #FFB86CA6;">UPDATE </mark>테이블 명 <mark style="background: #FFB86CA6;">SET</mark> 업데이트하려는 속성이름 = 값 WHERE 조건;
	- 예를 들어 조건에는 id =1; 과 같은 값이 들어갈 수 있음
	- SELECT * FROM 테이블 명 WHERE 조건;
	- 사용 예) UPDATE employee SET salary = salary * 2 WHERE dept_id = 1003;
	- 사용 예 2) UPDATE employee, works_on SET salary = salary * 2 WHERE<mark style="background: #FFB86CA6;"> id = empl_id</mark> and proj_id = 2003;
		- id = empl_id 가 두 테이블을 연결짓는 연결고리 역할
		- WHERE employee.id = works_on.empl_id 로 표현할 수 도 있음
- 데이터 삭제하기
	- <mark style="background: #FFB86CA6;">DELETE FROM</mark> 테이블 명 WHERE 조건;
	- DELETE FROM works_on WHERE impl_id = 5 and proj_id <> 2001;
		- <> 2001은 '2001을 제외하고' 라는 의미
		- != 2001; 도 같은 의미
## 데이터 조회하기 
- <mark style="background: #FFB86CA6;">SELECT</mark> name, position FROM employee WHERE id = 9;
- SELECT employee.id, employee.name, position FROM project, employee WHERE project.id = 20002 and project.leader_id = employee.id;
	- project.id = 2002 이 부분은 selection condition 선택 조건
	- project.leader_id = employee.id; 이 부분은 두 테이블을 연결하는 join condition
	- and로 묶였기 때문에 두 조건이 모두 만족되어야 함
	- join condition에 의해서 두 테이블의 튜플이 모두 선택되는 것
## AS 사용하기
- 테이블이나 attribute에 별칭을 붙일 때 사용함
- 이는 생략 가능
- SELECT employee.id, employee.name, position FROM project, employee WHERE project.id = 20002 and project.leader_id = employee.id;
	- 여기서 테이블 명이 너무 길어지면 가독성도 떨어지고 번거로운 문제 발생
	- SELECT E.id, E.name, position FROM project <mark style="background: #FFB86CA6;">AS</mark>  p, employee <mark style="background: #FFB86CA6;">AS</mark> E WHERE P.id = 20002 and P.leader_id = E.id;
## DISTINCT
- SELECT P.id, P.name FROM employee AS E, works_on AS W, project AS P WHERE E.position = 'DSGN' and E.id = W.empl_id and W.proj_id = P.id;
	- 궁금한 조건 1. 디자이너들이 어떤 프로젝트에 참여하고 있었는가
	- 2. works_on 이 employee와 project 테이블을 연결하는 연결고리 역할
		- E.id = W.empl_id and W.proj_id = P.id;
- 위 결과로 도출되는 값들은 중복 값도 같이 표시됨
- 이때  SELECT <mark style="background: #FFB86CA6;">DISTINCT</mark> P.id, P.name FROM employee AS E, works_on AS W, project AS P WHERE E.position = 'DSGN' and E.id = W.empl_id and W.proj_id = P.id;
	- DISTINCT를 넣으면 중복 제거
## LIKE 키워드 사용하기
- SELECT name, FROM employee WHERE name LIKE 'N%' or name LIKE '%N';
	- 'N%' : 이름이 N으로 시작할 때
	- %N' : 이름이 N으로 끝날 때
- SELECT name, FROM employee WHERE name LIKE '%NG%';
	- '%NG': NG가 들어가는 이름
- SELECT name, FROM employee WHERE name LIKE 'J___';
	- 이름에 J가 들어가고 4글자인 이름
## escape 문자와 함께 LIKE 사용하기
- %로 시작하거나 _ 로 끝나는 프로젝트 이름을 찾고 싶은 경우
	- 문자 그대로 사용하고 싶은 경우 
	- SELECT name FROM project WHERE name LIKE '<mark style="background: #ABF7F7A6;">\%</mark>%' or name LIKE '% <mark style="background: #ABF7F7A6;">\_</mark>'
## 쿼리 안에 쿼리(subquery)
- ID가 14인 임직원보다 생일이 빠른 임직원의 ID, 이름, 생일을 알고 싶다.
	- 먼저 ID가 14인 임직원의 생일 찾기
	- SELECT birth_date FROM employee WHERE id = 14;
	- 그렇게 구한 생일보다 빠른 임직원의 ID, 이름, 생일 구하기
	- SELECT id, name, birth_date FROM employee WHERE birth_date < '1999-08-04';
	- 이 과정을 한 번에 수행할 수는 없을까?
- SELECT id, name, birth_date FROM employee WHERE birth_date < (SELECT birth_date FROM employee WHERE id = 14);
- subquery
	- SELECT, INSERT, UPDATE, DELETE에 포함된 query
	- subquery는 ( )안에 기술된다.
- outer query
	- main query라고도 불림
	- subquery를 포함하는 query

- ID가 1인 임직원과 같은 부서, 같은 성별인 임직원들의  ID와 이름과 직군을 알고싶다.
	- SELECT id, name, position FROM employee WHERE (dept_id, sex) = (SELECT dept_id, sex FROM employee WHERE id = 1);
	  
- ID가 5인 임직원과 같은 프로젝트에 참여한 임직원들의 ID를 알고싶다.
	- SELECT proj_id FROM works_on WHERE empl_id = 5;
		- 먼저 ID가 5인 임직원이 참여한 프로젝트 id 구하기
	- SELECT DISTINCT empl_id FROM works_on WHERE empl_id !=5 AND (proj_id = 2001 OR proj_id = 2002);
		- ID가 5인 임직원은 제외하고 2001이나 2002 프로젝트에 참여한 empl_id를 구함
	- 이를 간단히 하기
		- SELECT DISTINCT empl_id FROM works_on WHERE empl_id !=5 AND proj_id IN (2001, 2002);
	- 두 쿼리 합치기
		- SELECT DISTINCT empl_id FROM works_on WHERE empl_id !=5 AND proj_id IN (SELECT proj_id FROM works_on WHERE empl_id = 5;);
	- IN ( ) 의 의미
		- in 뒤 괄호 안에 있는 것 중 하나와 값이 같다면 TRUE를 return
		- NOT IN ( )은 괄호 안에 있는 모든 값과 값이 다르다면 TRUE를 return

- ID가 5인 임직원과 같은 프로젝트에 참여한 임직원들의 ID와 이름을 알고싶다.
	- SELECT id, name FROM employee WHERE id IN (SELECT DISTINCT empl_id FROM works_on WHERE empl_id !=5 AND proj_id IN (SELECT proj_id FROM works_on WHERE empl_id = 5;);)
	- 다르게 표현하기
		-  SELECT id, name FROM employee,<mark style="background: #FFF3A3A6;"> (SELECT DISTINCT empl_id FROM works_on WHERE empl_id !=5 AND proj_id IN (SELECT proj_id FROM works_on WHERE empl_id = 5))</mark> AS DSTINCT_E WHERE id = DSTINCT_E.empl_id;
			- FROM절에 서브쿼리를 넣어 가상의 테이블을 만든 뒤 조인 컨디션으로 employee id와 가상 테이블의 id를 연결한 후 id 와 name select

- ID가 7 혹은 12인 임직원이 참여한 프로젝트의 ID와 이름을 알고싶다.
	- SELECT P.id, P.name FROM project AS P WHERE EXISTS (SELECT * FROM works_on AS W WHERE W.proj_id = P.id AND W.empl_id IN (7,12) );
	- EXISTS: 서브쿼리의 결과가 최소 하나의 row라도 있다면 true를 반환
	- NOT EXISTS: 서브쿼리의 결과가 단 하나의 row도 없다면 TURE를 반환

- 2000년대 생이 없는 부서의 ID와 이름을 알고싶다.
	- SELECT D.id, D.name FROM department AS D WHERE D.id NOT IN (SELECT E.dept_id FROM employee AS E WHERE E.birth_date >= '2000-01-01' );
	- SELECT D.id, D.name FROM department AS D WHERE NOT EXISTS (SELECT * FROM employee AS E WHERE E.dept_id = D.id AND E.birth_date >='2000-01-01')

- 부서의 리더보다 높은 연봉을 받는 부서원을 가진 리더의 ID와 이름, 연봉을 알고싶다.
	- SELECT E.id, E.name, E.salary FROM department AS D employee AS E WHERE D.leader_id = E.id AND E.salary < ANY (SELECT salary FROM employee WHERE id <> D.leader_id AND dept_id = E.dept_id  )
		- WHERE id <> D.leader_id: 리더의 아이디가 아닌 직원의 아이디
		- dept_id = E.dept_id: 리더의 부서 아이디와 같은 부서
		- ANY: 단 하나라도 작은 경우가 있다면
- 여기서 도출된 결과에서 그 부서의 최고 연봉은 얼만지 구하고 싶을 때
	- SELECT E.id, E.name, E.salary, <mark style="background: #FFF3A3A6;">( SELECT max(salary) FROM employee WHERE dept_id = E.dept_id )</mark> AS dept_max_salary FROM department D, employee E WHERE D.leader_id = E.id AND E.salary < ANY (SELECT salary FROM employee WHERE id <> D.leader_id AND dept_id = E.dept_id);
	- SELECT 절에도 서브쿼리가 추가될 수 있다 -> 도출된 튜플에 속성이 추가돼서 도출

- ID가 13인 임직원과 한 번도 같은 프로젝트에 참여하지 못한 임직원들의 ID, 이름, 직군을 알고싶다.
	- SELECT DISTINCT E.id, E.name, E.position FROM employee E, works_on W WHERE E.id = w.empl_id AND proj_id <> ALL (SELECT proj_id FROM works_on WHERE empl_id = 13);
	- SELECT proj_id FROM works_on WHERE empl_id = 13
		- id 가 13인 직원이 참여한 프로젝트 아이디 구하기
	- <> ALL : 그 어떤 것과도 같지 않다면

## Three-valued logic
- SQL에서 NULL의 의미
	- unknown
	- unavailable or withheld
	- not applicable
- SQL에서 NULL과 비교 연산을 하게 되면 그 결과는 unknown
- unknown은 TRUE일수도 FALSE일수도 있다는 의미
- three-valued logic: 비교/논리 연산의 결과로 TRUE/FALSE/UNKNOWN을 가진다.

- AND
	- TRUE AND UNKNOWN => UNKNOWN
	- FALSE AND UNKNOWN => FALSE
	- UNKNOWN AND UNKNOWN => UNKNOWN
- OR
	- TRUE OR UNKNOWN => TRUE
	- FALSE OR UNKNOWN => UNKNOWN
	- UNKNOWN OR UNKNOWN => UNKNOWN
- NOT
	- UNKNOWN NOT UNKNOWN => UNKNOWN
## WHERE 절의 condition
- where절에 있는 condition의 결과가 TRUE인 튜플만 선택됨
- 즉 결과가 FALSE거나 UNKNOWN이면 튜플은 선택되지 않는다.
## NOT IN 사용 시 주의사항
- v NOT IN (v1, v2, v3)은 아래와 같은 의미
- v != V1 AND v != v2 AND v != V3
- 만약 v1, v2, v3 중 하나가 NULL이라면?
- 3 not in (1,2,NULL) => UNKNOWN
## 테이블 조인
- SQL에서 JOIN
	- 두 개 이상의 table들에 있는 데이터를 한 번에 조회하는 것
	- 여러 종류의 join이 존재함
### implicit join VS explicit join
- SELECT D.name FROM employee AS E, department AS D WHERE E.id = 1 <mark style="background: #FFF3A3A6;">AND E.dept_id = D.id;</mark>
	- implicit join: 위 경우 처럼 where절에 join condition이 같이 있는 경우
		- from 절에는 table들만 나열하고 where절에 join condition을 명시하는 방식
		- where 절에 selection condition과 join condition이 같이 있기 때문에 가독성이 떨어진다는 단점
- SELECT D.name FROM employee AS E JOIN department AS D ON E.dept_id = D.id WEHRE E.id = 1;
	- explicit join: from 절에 키워드와 함께 joined table들을 명시하는 방식
	- from 절에서 on 뒤에 join condition이 함께 명시된다
	- 가독성이 좋다
### inner join VS outer join
- SELECT * FROM employee E JOIN department D ON E.dept_id = D.id;
	- 이는 실제로 JOIN 앞에 INNER가 생략된 것
	- 두 테이블에서 조인 컨디션을 만족하는 튜플들로 result table을 만드는 조인

- outer join
	- 두 테이블에서 조인 컨디션을 만족하지 않는 튜플들도 result table에 포함하는 조인
	- LEFT OUTER JOIN/ RIGHT OUTER JOIN/ FULL OUTER JOIN
	- LEFT OUTER JOIN은 왼쪽에 있는 테이블 중에서 조인 컨디션에 의해서 매칭되지 않은 튜플도 포함시키는 것

- equi join
	- inner/ outer 상관없이 = 을 사용한 join이라면 equi join으로 보는 경우
	- inner join으로 한정해서 =을 사용한 경웽 equi join으로 보는 경우
- using
	- 두 table 이 equi join 할 때 join 하는 attribute의 이름이 같다면 using으로 간단하게 작성 가능
### natural join
- 두 table에서 같은 이름을 가지는 모든 attribute pair에 대해서 equi join을 수행
- join condition을 따로 명시하지 않음
- SELECT * FROM employee E NATURAL INNER JOIN department D;
- SELECT * FROM employee E INNER JOIN department D ON E.dept_id = D.dept_id AND E.name = D.name; 과 같음

### cross join
- 두 table의 tuple pair로 만들 수 있는 모든 조합(= cartesian product )을 result table로 반환
- join condition이 없다
- SELECT * FROM employee CROSS JOIN department;
- MYSQL에서 CROSS JOIN
	- MYSQL에서는 cross join = inner join = join
	- cross join 에 on(or using)을 같이 쓰면 inner join으로 동작한다
	- inner join이 on(or using) 없이 사용되면 cross join으로 동작한다.
### self join
- table이 자기 자신에게 join하는 경우

### join example
- ID가 1003인 부서에 속하는 임직원 중 리더를 제외한 부서원의 ID, 이름, 연봉을 알고싶다.
	- SELECT E.id, E.name, E.salary FROM employee E INNER JOIN department D ON E.dept_id = D.id where E.dept_id = 1003 and E.id != D.leader_id;
- ID가 2001인 프로젝트에 참여한 임직원들의 이름과 직군과 소속 부서 이름을 알고싶다.
	- SELECT E.name, E.position, D.name FROM works_on W JOIN employee E ON W.empl_id = E.id LEFT JOIN department D ON E.dept_id = D.id WHERE W.proj_id = 2001;
## 그룹 짓기(grouping), 집계함수(aggregate function), 정렬하기(ordering)
- 각 프로젝트 별로 프로젝트에 참여한 90년대 생들의 수와 이들의 평균연봉을 알고싶다.
	- SELECT proj_id, COUNT( * ), ROUND(AVG(salary), 0) FROM works_on W JOIN employee E ON W.empl_id = E.id WHERE E.birth_date BETWEEN '1990-01-01' AND '1999-12-31' GROUP BY W.proj_id
- 프로젝트 참여 인원이 7명 이상인 프로젝트에 한정해서 각 프로젝트 별로 프로젝트에 참여한 90년대 생들의 수와 이들의 평균 연봉을 알고싶다.
	- SELECT proj_id, COUNT( * ), ROUND(AVG(salary), 0) FROM works_on W JOIN employee E ON W.empl_id = E.id WHERE E.birth_date BETWEEN '1990-01-01' AND '1999-12-31' <mark style="background: #FFF3A3A6;">AND W.proj_id IN (SELECT proj_id FROM works_on GROUP BY proj_id HAVING COUNT( * ) >= 7 ) </mark>GROUP BY W.proj_id ORDER BY W.proj_id;

### ORDER BY
- 조회 결과를 특정 attribute 기준으로 정렬하여 가져오고 싶을 때 사용
- default 정렬 방식은 오름차순
- 오름차순 정렬은 ASC
- 내림차순 정렬은 DESC
- SELECT * FROM employee ORDER BY salary;

### aggregate function
- 여러 튜플들의 정보를 요약해서 하나의 값으로 추출하는 함수
- 대표적으로 COUNT, SUM, MAX, MIN, AVG 함수가 있다.
- 관심있는 attribute에 사용된다.
- NULL 값은 제외하고 요약 값을 추출한다.

- 프로젝트 2002에 참여한 임직원 수와 최대 연봉과 최소연봉과 평균 연봉을 알고싶다.
	- SELECT COUNT( * ), MAX(salary), MIN(salary), AVG(salary) FROM works_on W JOIN employee E ON W,empl_id = E.id WHERE W.proj_id = 2002;
- 각 프로젝트에 참여한 임직원 수와 최대 연봉과 최소 연봉과 평균 연봉을 알고싶다.
	- SELECT W.proj_id, COUNT( * ), MAX(salary), MIN(salary), AVG(salary) FROM works_on W JOIN employee E ON W,empl_id = E.id WHERE GROUP BY W.proj_id;
	- GROUP BY에 사용된 attribute는 SELECT에 반드시 적어줘야 함
### GROUP BY
- 관심있는 attribute 기준으로 그룹을 나눠서 그룹별로 aggregate function을 사용하고 싶을 때 사용
- HAVING
	- GROUP BY와 함께 사용
	- aggregate function의 결과값을 바탕으로 그룹을 필터링하고싶을 때 사용
	- having 절에 명시된 조건을 만족하는 그룹만 결과에 포함된다.
## SELECT 요약
- SELECT attribute or aggregate function FROM table WHERE conditions GROUP BY group attribute HAVING group condition ORDER BY attribute;

- 실행순서
	- FROM -> WHERE -> GROUP BY -> HAVING -> ORDER BY -> SELECT




















## 자바스크립트 생성자함수
- 자바스크립트 객체 개념
- class 생성, 생성자 함수에서 this가 왜 필요한가

- class HashData {
  constructor(key, value) {
    this.key = key;
    this.value = value;
  } } 
 이 코드는 HashData 객체를 만드는 설계도
-  여기서 const data = new HashData('age', 20); 이 같은 코드가 실행되면
 새 객체가 하나 생성됨
- 그 새 객체를 가리키는 것이 this
- this.key = key; 를 말로 풀면 지금 만들어지고 있는 객체 this에 key라는 속성을 만들고 생성자에서 받은 key 값을 저장해라 
- this 없이 key = key라고 해버리면 그냥 변수끼리 대입하는 것으로 객체 x

## 자바스크립트 
- class HashTable {
  constructor() {
    this.arr = [];
    for (let i = 0; i < 10; i++) {
      this.arr[i] = new DoublyLinkedList();
    } } }
- HashTable이라는 객체를 하나 만들 때
- 내부에 arr이라는 배열을 만들고
- 그 배열의 0~9번 칸에 (for 반복문)
- 각각  DoublyLinkedList 객체를 하나씩 넣어라

- 왜 배열 안에  DoublyLinkedList를 넣는가
	- 해시 테이블에서 해시 충돌을 해결하기 위해
	- 같은 해시값 -> 같은 인덱스 충돌 발생 -> 그 칸의 연결리스트에 저장
	- 이 생성자는 HashTable 객체를 만들 때, 내부에 고정 크기(10)의 배열을 만들고, 각 칸을 비어 있지 않도록 연결 리스트 객체로 초기화하기 위해 this를 사용
	- 

## 자바스크립트 메서드
- 메서드는 객체에 소속된 함수를 메서드라고 부름
- hashFunction(number) {
  return number % 10;}
- 이 메서드 즉 함수가 필요한 이유는 값을 넣을 위치 (인덱스)를 계산하기 위해서
- 위에서 HashTable 객체 설계도를 만들면서 내부 배열의 크기를 0~9 10칸으로 정했기 때문에 숫자를 받아서 0~9사이의 번호로 바꿔줄 해시 함수가 필요
- 예를 들어 테이블에 42라는 값을 저장하려 할 때 배열의 몇 번 칸에 넣을지 의문이 생김
- 이를 정해주기 위해 위 메서드가 필요한 것
- hashFunction 메서드가 Hashtable class 안에 정의되어 있는 이유는 해시 함수가 HashTable전용 로직이기 때문
- 다른 곳에서 사용되는 일반 함수가 아님

## 자바스크립트 class
- class (설계도)
  ↓ new 로 객체 생성
  객체 (인스턴스)
  ├─ 데이터 (arr)
  ├─ 메서드 (hashFunction)

## 해시테이블의 저장 동작 메서드

```this.arr[this.hashFunction( key )].insertAt(0,new HashData(key,value));}```

- 한 문장으로 정리하면 key와 value를 받아서 key로 위치를 계산하고 그 위치에 해당하는 연결리스트에 (key,value) 데이터를 저장한다.
- this.hashFunction(key)는 입력된 key를 숫자로 변환한  값 -> 0~9 사이의 수 
- this.arr[해시값] 은 hashTable 생성자에서 만들어 둔 구조 -> 해시값에 해당하는 연결리스트를 의미
- .insertAt(0, new HashData(key, value))에서 일단 insertAt 괄호 안에 들어가는 값은 index와 data임 -> 앞에서 정의함
- 그렇다면 0이 인덱스가 되고 뒤에 new HashData(key, value)가 데이터가 되는 것
- new HashData(key, value)이 실제로 저장되는 데이터 객체가 됨
- 해시 테이블은 key만으로 찾고 실제 데이터는 이 객체에 저장

- 전체 동작을 순서도로 표현
- set(42, "hello")
   ↓
  hashFunction(42) → 2 (인덱스 설정)
   ↓
  arr[2] 선택 (연결 리스트)
   ↓
  HashData { key: 42, value: "hello" } 생성
   ↓
  연결 리스트 맨 앞에 삽입
- 따라서 실제로 저장되는 방식은 다음과 같음
  arr[2] = [
  { key: 22, value: "B" },
  { key: 12, value: "A" }]

## 해시테이블의 get 메서드
```get(key){
let currentNode = this.arr[this.hashFunction(key)].head;
while (currentNode !== null){
if (currentNode.data.key === key)
	{return currentNode.data.value;}
 currentNode = currentNode.next;}
return null;} ```
```
- 해시테이블의 조회 동작(get)
- 이 메서드의 역할은 주어진 key에 대응하는 value를 찾아서 반환하는 것, 없으면 null 반환
- let currentNode = this.arr[this.hashFunction(key)].head;
	- 이 한 줄에서 일어나는 일은
	- 먼저 this.hashFunction(key)에서 key를 숫자로 반환 -> 결과 0~9
	- this.arr[해시값] 에서 해당 인덱스의 연결리스트 선택 
	- .head로 그 연결리스트의 첫 번째 노드 선택
	- 즉, key가 들어 있을 가능성이 있는 연결 리스트의 맨 앞 노드부터 탐색하겠다
	- while (currentNode !== null) { 에서 currentNode가 존재하는 동안 반복하고 연결리스트의 끝에 도달하면 null -> 노드가 있는 동안 계속 검사
	- if (currentNode.data.key === key) {  return currentNode.data.value; }에서 currentNode.data.key는 저장된 key를 의미하고 key는 찾고 싶은 key를 의미 -> 둘의 값이 같으면 value를 반환하고 즉시 종료
	- currentNode = currentNode.next; 에서 현재 노드를 버리고 다음 노드로 이동 -> 연결리스트를 순회하며 값을 찾는 것
	- return null; 끝까지 값을 찾지 못했을 경우 -> 해당 해시 인덱스에 key가 존재하지 않음 -> 이 key에 대응하는 값은 없다
	- 


